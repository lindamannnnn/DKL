# 课件测评报告 v2（复测）：小柯 × GESP3-01 一维数组

> ⚠️ **基线声明：本次评估以 GESP1+GESP2 毕业基线作为已掌握知识**（`gesp1-graduate.md` + `gesp2-graduate.md`），凡是基线里已有的知识点（for/while、自定义函数、全局变量、`abs`、打擂台式找最值、`#include <bits/stdc++.h>` 万能工具箱等）不算超纲。
>
> ⚠️ **开卷声明：示例代码（demo）无法隐去，属开卷，完成度可能高估。** 我已按三步法先"遮住示例"在脑子里独立作答再对答案，但"独立完成"结论仅供参考。
>
> 测评人：小柯（9 岁三年级）｜ 复测日期：2026-07-21 ｜ 课件：`server/courses/gesp3/01-一维数组.md`（全文 395 行，已分段读完）
>
> 🔁 **复测背景**：v1 报告（`eval-xiaoke-gesp3-01-v1.md`）指出 2 个已修复问题——① "数组大小必须是固定数字"规则已补进"数组的定义"卡片（原文第 50 行）；② "遍历"已补解释（原文第 164 行）。本次重点验证这两处修复是否有效，并首次对 5 道课后题（用快照 `snapshots/problems-gesp3-01.md`）执行三步法，分母补全为 12。

---

## 测试课程：GESP3-01 一维数组

### 逐页体验记录（按 `## ` 分页，先看标题和图 → 逐句读 → 默问"懂了吗"）

- **故事：成绩怎么存？** "写 50 个变量手酸"我秒懂——我以前最多循环读 T 组数据，读完就扔，真没想过要"存起来"。想往下读。
- **回顾** `for (int i = 1; i <= n; i++)` 我会（GESP1 第 10 课），过渡自然。
- **卡片：什么是数组** "一排编了号的储物柜" 🗄️ 好懂，像我们教室后面的书包柜。
- **卡片：数组的定义** `int a[5];` 看得懂。⭐ 新加的警告我逐字读了："大小必须是一个固定的数字，不能写变量！`int a[n];` 是错误的"——还说了为什么（"电脑申请柜子的时候就要知道一共几个"），还教了比赛习惯 `int a[105];`。**v1 我就是卡在这里的，这次直接记住了。** demo 9 行有点长，但注释能看懂。
- **检查点：哪种对？** 闭卷选 B，方括号是柜子的编号牌。
- **卡片：下标从 0 开始** 从 0 数有点别扭，但"5 个柜子是 0 到 4 号"举了例子。"越界会读到乱糟糟的数据"有点怕，记住了别写 `a[5]`。
- **检查点：第 5 个呢？** 闭卷选 B `a[4]`。题干里"元素"这个词前文没出现过，我联系上下文猜是"柜子里的数"，猜对了，但确实是猜的。
- **卡片：数组初始化** 三种装法都有例子，`{0}` 清零好方便。"写在 main 外面的数组自动变 0"——全局变量我学过（GESP1-10、GESP2-02），接得上。
- **检查点：总和是几？** 闭卷选 B：0+5+0+0=5。
- **卡片：循环遍历数组** ⭐ "遍历"这次有解释了："一个一个全都看一遍，这个动作就叫遍历（'遍'就是'全部'的意思）"——词和动作一起懂了，不用猜。下标从 1 开始"和题目说法一一对应"这个理由说服我了。
- **检查点：循环到哪？** 闭卷选 A `i <= n`，`< n` 会漏掉最后一个柜子。
- **卡片：查找最大值** 打擂台 🥊 好懂，而且我 GESP1 第 13 课就学过"先读第一个当老大"，旧知识搬进柜子，接上了。
- **卡片：最大值的位置** "多带一个小本本 `pos`"，一次只多一个新东西，跟得上。
- **检查点：哪个位置？** 闭卷选 A：平手不算赢，擂主不换人，所以是第一个。全课最绕的一题，多想了一会儿。
- **挑战 1：数组输入输出** ⭐ **v1 的真实卡点消失了**：这次闭卷我直接写 `int a[105];`，没有犹豫——定义卡那条"不能写变量"的规则我记得清清楚楚。
- **挑战 2：最大值的位置** 擂主 + 小本本直接默写出来，没卡。
- **小贴士** 两条都和前面呼应（余量、全局自动 0），读完有"原来如此"的感觉。
- **课后挑战** 只有编号，题面要看快照文件（数据库导出），这个 v1 就提过。

### 客观测量

**A. 每页最长连续中文字符数（目标 ≤80，不含代码/英文/数字）**

| 页 | 最长连续中文字数 | 达标？ |
|---|---|---|
| 故事 | ≈24（"要是有一个大柜子能把个成绩按编号全放进去就好了"） | ✅ |
| 回顾 | ≈8 | ✅ |
| 什么是数组 | ≈20 | ✅ |
| 数组的定义（含新增规则句） | ≈20（新增句"中括号里的大小必须是一个固定的数字不能写变量"逐句短，没有连成超长句） | ✅ |
| 下标从 0 开始 | ≈21 | ✅ |
| 数组初始化 | ≈22 | ✅ |
| 循环遍历数组（含新增解释句） | ≈17（"一个一个全都看一遍这个动作就叫遍历"） | ✅ |
| 查找最大值 | ≈17 | ✅ |
| 最大值的位置 | ≈18 | ✅ |
| 挑战 1 / 挑战 2 教师卡 | ≈21（"有两个相同最大值时题目要第一个位置所以判断用"） | ✅ |
| 小贴士 / 课后挑战 | ≈15 | ✅ |

- **结论：全部页面达标。** 新增的两处修复文字都是短句，没有把页面撑密。

**B. 未解释新术语清单**（对照 GESP1+GESP2 基线逐个核对）

1. ~~遍历~~ —— **已修复**：第 164 行明确解释（"一个一个全都看一遍，这个动作就叫遍历"），从清单移除，见"修复验证"。
2. **元素**（检查点 2 题干，原文第 111 行，**未引入**）——前文只说"柜子""第几个"，题干突然冒出"元素"。我联系上下文猜对了，但这是猜的不是教的。（轻微）
3. **越界**（原文第 89 行，半解释）——靠"会读到乱糟糟的数据"这个后果猜懂意思，词本身没定义。可接受，不扣分。
4. `#include <bits/stdc++.h>` —— 基线已有（GESP1-01"万能工具箱"），不算新词（v1 已勘误）。

**C. 新授示例代码行数（目标 ≤8 行，按完整代码块计）**

| 页 | 示例 | 行数 | 达标？ |
|---|---|---|---|
| 数组的定义 demo（原文 54-64 行） | 新授 | **9 行** | ❌ 超标 |
| 下标 demo（93-102 行） | 新授 | 8 行 | ✅ |
| 初始化 demo（133-141 行） | 新授 | 7 行 | ✅ |
| 循环遍历 demo（170-180 行） | 新授 | **9 行** | ❌ 超标 |
| 查找最大值 demo（207-217 行） | 新授 | **9 行** | ❌ 超标 |
| 最大值位置 demo（231-241 行） | 新授 | **9 行** | ❌ 超标 |
| 挑战 1 / 2 示例 | 练习 | 9 / 12 行 | （非新授，不计） |

- **结论：6 个新授示例中 4 个为 9 行，各超标 1 行**（与 v1 相同，未修）。照着打能打完，但规则就是规则。

### 独立完成测试（三步法）

分母 = 5 checkpoint + 2 挑战 + 5 课后题（快照）= **12**。

| 题目 | 闭卷结果 | 对答案差异 | 判定 |
|---|---|---|---|
| CP1 哪种对？（69-78 行） | 闭卷选 B `int a[5];` | 0 处 | ✅ |
| CP2 第 5 个呢？（107-116 行） | 闭卷选 B `a[4]`，从 0 数：0,1,2,3,4 | 0 处 | ✅ |
| CP3 总和是几？（146-155 行） | 闭卷选 B 5：`{0}` 清零后只有 2 号柜是 5 | 0 处 | ✅ |
| CP4 循环到哪？（185-194 行） | 闭卷选 A `i <= n` | 0 处 | ✅ |
| CP5 哪个位置？（246-255 行） | 闭卷选 A 第一个：平手不换擂主 | 0 处 | ✅ |
| 挑战 1 数组输入输出（259-311 行） | ⭐ 闭卷直接写 `int a[105];` + 两个 for 循环读写，**没有卡**——"大小必须是固定数字"的规则这次在定义卡就学了 | 0 处 | ✅（v1 为 🟡） |
| 挑战 2 最大值的位置（315-369 行） | 闭卷默写 `maxv=a[1]; pos=1;` + 擂台循环，输出 `91 3` 对样例 | 0 处 | ✅ |
| 课后 1 人数统计（快照题 1） | 闭卷写 `int cnt[5] = {0};`，读一个分按 `s<60 / s<70 / ...` 分到 5 个桶 `cnt[k]++`，最后逐行输出。计数和多分支都是旧知识，数组桶是刚学的 `{0}` 初始化，样例心算对上（53→1；65,68→2；80,87→2；90,100→2） | 0 处 | ✅ |
| 课后 2 找数组的最大数（快照题 2） | 闭卷写擂台循环只求最大值，是挑战 2 的简化版 | 0 处 | ✅ |
| 课后 3 找最小数的位置（快照题 3） | 闭卷把擂台方向反过来：`minv=a[1]; pos=1; if (a[i]<minv)`，严格 `<` 保住第一个最小值（CP5 刚想过这个） | 0 处 | ✅ |
| 课后 4 奇数位偶数位和与差（快照题 4） | 闭卷**第一反应写成 `a[i] % 2`（判断数值的奇偶）**，重读题面括号警告"不是奇数和偶数的和，是奇数**位**"后改成 `i % 2 == 1` 累加 `sum1`、否则 `sum2`，最后大减小。被题面提示捞回来 1 次 | 1 处（判奇偶的对象搞错） | 🟡 |
| 课后 5 排名第二高的成绩（快照题 5） | 闭卷卡住："第二高"没学过。想"把最大的删掉"——不会删；改用样例反推出"先擂台找 99，再扫一遍跳过 99 找最大的"，但 `m2` 初始化拿不准（挑战 2 教师卡刚警告过 `maxv` 不能乱写 0），对着样例（98 出现多次、99 只一次）验证 `if (a[i] != maxv && a[i] > m2)` 才敢写 | 思路对但初始化靠样例反推 | 🟡 |

- **完成度 = (10✅×1 + 2🟡×0.5) ÷ 12 = 11 ÷ 12 ≈ 0.9**（按公式计算，保留 1 位小数；开卷状态可能高估）

### 卡点日志

- **[挑战 1]（v1 卡点复测）**：v1 记录"不知道数组大小能不能写 n，盯了约 1 分钟 → 回看示例"。本次同一位置**无任何停顿**，直接写 `int a[105];`。卡点消除。
- **[课后题 4]** 卡住：把"奇数位"看成"奇数"，写了 `a[i] % 2`，算了样例第一个数 4 发现对不上（4 是偶数却在奇数位和 24 里）→ 触发 重读题面括号警告，约 30 秒后改正为 `i % 2`。
- **[课后题 5]** 卡住："第二高"怎么找没学过，盯着样例想约 1 分钟无进展 → 触发 用样例反推思路（先找最大，再排除最大找最大），初始化 `m2` 仍不确定。
- 其余页面与题目无卡点，不编造。

### 修复验证（本次复测核心）

**修复 ①："数组大小必须是固定数字"规则（原文第 50 行，定义卡新增⚠️句）——有效 ✅**
- 证据：规则现在和 `int a[5];` 的定义**同一张卡**出现，还给了错误示范 `int a[n];` 和比赛习惯 `a[105]`。
- 效果：挑战 1 闭卷不再卡（v1 🟡 → v2 ✅），课后 1/2/3 闭卷也全部直接写固定大小数组，没有一次想写 `a[n]`。

**修复 ②："遍历"解释（原文第 164 行新增）——有效 ✅**
- 证据："一个一个全都看一遍，这个动作就叫**遍历**（'遍'就是'全部'的意思）"。
- 效果：**"遍历"不再算未解释新术语**，已从客观测量 B 清单移除；读标题"循环遍历数组"时词和动作能同时懂，不用靠正文反猜。

### 评分

- **理解率：100**（全懂。证据：5 道 checkpoint 闭卷全对；挑战 1/2 闭卷独立写出；v1 两个理解障碍——数组大小规则、"遍历"——修复后均在闭卷中正确使用；逐页阅读无"连续 3 句看不懂"。仅剩"元素"一词靠猜，但 CP2 闭卷答对证明不妨碍理解概念本身，属措辞瑕疵而非理解缺口）
- **完成度：0.9**（按公式 (10×1+2×0.5)/12=11/12，证据见三步法表；开卷状态可能高估）
- **是否通过：✅ 通过**（小柯线：理解率 ≥75 且完成度 ≥0.8，两项均达标；v1 压线通过，本次余量明显变大）

### 至少 3 个必须修改的问题

1. **4 个新授示例仍为 9 行，超过 ≤8 行目标**（证据：定义 demo 54-64 行、遍历 demo 170-180 行、擂台 demo 207-217 行、位置 demo 231-241 行，每段恰超 1 行）。建议每段删 1 行可省的注释或空行；v1 已提，本次复测仍未修。
2. **"元素"未引入就先使用**（证据：检查点 2 题干第 111 行"第 5 个元素"，前文 1-104 行只有"柜子""第几个"两种说法）。建议在"什么是数组"卡片补半句："柜子里的数据，正式的叫法是**元素**。"
3. **课后题 5"排名第二高的成绩"超出本课教学范围**（证据：课件第 198-242 行只教"一个擂主"找最大值和位置；快照题 5 要求"第二高"，需要"排除最大值再擂台"或"双擂主"的未教思路，闭卷只能靠样例反推判 🟡）。建议换成单擂主变式（如"最小值和它的位置"），或在课件补一小节"怎么找第二大的"。
4. （流程类，非课件内容）课后 5 题只以 UUID 引用存在（原文 391-395 行），课件本身不含题面，测评必须先从数据库导出快照才能进行三步法——本次已靠 `snapshots/problems-gesp3-01.md` 解决，但建议测评流程把"导出快照"固定为前置步骤（本次已执行）。

### 一句话总结（用角色口吻）

"上次卡住我的两个地方都修好啦——柜子大小要写固定数字我记住了，'遍历'就是'全部看一遍'我也懂了！就是'第二名怎么找'还没人教我，我自己想了半天。"

### 评分自检

- [x] 每个分数旁边都有 1 行证据？（理解率、完成度均已附证据）
- [x] 完成度是按公式算的，不是估的？（(10×1+2×0.5)/12=11/12≈0.9，列式计算）
- [x] 理解率是 0/25/50/75/100 之一？（100）
- [x] 至少列了 3 个具体问题？（列了 4 条，均带行号/题号证据）
- [x] 完成"闭卷"尝试了，还是开卷？开卷是否已声明？（每题先闭卷再对答案；报告开头已显著声明开卷、完成度可能高估）
- [x] 有没有把"看着示例能跑"误当成"理解了"？（没有——课后题 4、5 因闭卷需提示/反推判 🟡，未计入 ✅）
