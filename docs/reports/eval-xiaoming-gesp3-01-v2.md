# 小明测评报告：GESP3-01 一维数组（v2 复测）

- 测评人：小明（11 岁，五年级，虚拟体验官）
- 测评时间锚点：2026-07-21 11:40（UTC+8）
- 被测课件：`server/courses/gesp3/01-一维数组.md`（v2 修复版：题 5 换新、补下标口诀、打擂台 demo 补占位注释）
- 课后题快照：`docs/reports/snapshots/problems-gesp3-01.md`（题 5 已更新为"找找谁的身高超过全家的平均身高"）

> ⚠️ **本次评估以 GESP1 + GESP2 毕业基线作为已掌握知识**（`references/baselines/gesp1-graduate.md` + `gesp2-graduate.md`）。
> ⚠️ **课件中的示例代码（demo）无法隐去，本评估属开卷；"独立完成"结论按三步法闭眼模拟执行，完成度仍可能高估。**
> ⚠️ **本次为 v2 复测**：我上次学过这一课（v1 报告：理解率 75、完成度 0.7、不通过、不愿意继续），这次是复习性质重学，但仍按课件当前内容一页一页认真体验，不凭上次记忆打分。

---

## 测试课程：GESP3-01 一维数组

我的身份：小明。上次学这课，柜子、打擂台我都学会了，但下标 0 和 1 打架、最后一题两个擂主做不出来，徽章飞了。听说课件改了，我来复测——要是还坑我，我就……我就再跟小柯告状。

## 第 1 页：故事：成绩怎么存？

还是小柯写 50 个变量手酸 😵 的故事。上次看过，复习课我扫得快，但没跳页——储物柜比喻我记得，"一个名字管整排"这句我重新读了一遍。

- 本页最长连续中文：约 30 字（行 12）✅ 达标
- 新术语：数组（第 3 页解释）✅

## 第 2 页：回顾

`for (int i = 1; i <= n; i++)`，老熟人，翻页。

## 第 3 页：卡片：什么是数组

一排编了号的储物柜 🗄️。复习，懂，过。

- 最长连续中文：约 25 字 ✅

## 第 4 页：卡片：数组的定义

`int a[5];` 申请 5 个连在一起的柜子。⚠️ 警告（行 50）：大小必须是**固定的数字**，不能写 `int a[n]`——这条上次就补过了，这次重读还是清楚：电脑申请柜子时就要知道一共几个。比赛开大点 `int a[105]`，记住了。

demo 9 行（行 55-63），`a[0] = 90` 跳过 `a[1]` 直接 `a[2] = 70`。上次我愣了一下"1 号柜呢"，这次我知道是故意跳着放的，直接过。复习就是快，嘿嘿。

- 最长连续中文：约 63 字（行 50 警告段，去掉代码符号数的）✅ 达标（≤80，但这是全课最长的一段，贴边）
- 新授示例 9 行（≤10）✅

## 第 5 页：检查点：哪种对？

**闭卷**：B！`int a[5];` 中括号。秒选。
**对答案**：B ✅。白送题，我又比小柯快（他说的）。

## 第 6 页：卡片：下标从 0 开始

下标从 0 开始数，`a[0]` 到 `a[4]`。⚠️ 写 `a[5]` 就**越界**了（行 89）——我特意重看了这句，还是上次那个感觉："越界"只告诉我后果（不报错、读到乱糟糟的数据），词本身没解释。不过这次我有口诀垫底（第 10 页），知道是"越出柜子编号范围"的意思，自己脑补上了。还有"电脑不会报错"这句还是让我有点慌：不报错，错了谁告诉我？

- 新术语："越界"——⚠️ **仍只有后果、无词义解释**（行 89，v1 提过，没改）
- 新授示例 8 行（行 94-101）✅

## 第 7 页：检查点：第 5 个呢？

**闭卷**：第 5 个……定义从 0 数（口诀上场！），0、1、2、3、4，`a[4]`，选 B。上次我手都放到 A 上了，这次口诀一背就不晃了。
**对答案**：B ✅。这次不是悬崖边拉回来的，是稳稳选对的。

## 第 8 页：卡片：数组初始化

`{1, 2}` 装前 2 个其余补 0，`{0}` 全 0，全局数组自动全 0。复习，规律我还记得，过。

- 新授示例 7 行（行 134-140）✅

## 第 9 页：检查点：总和是几？

**闭卷**：`int a[4] = {0}; a[1] = 5;` → 0、5、0、0，总和 5，选 B。心算。
**对答案**：B ✅。

## 第 10 页：卡片：循环遍历数组（重点复测页）

"遍历"有解释（行 164："一个一个全都看一遍"），上次就会了。

**重点来了**：💡 提示下面多了一段 🧠 **口诀**（行 168）："**定义从 0 数，比赛从 1 用**——柜子编号天生从 0 开始；做题时我们故意从 1 号柜开始用，0 号柜空着占位，这样不会和'第几个'搞混。"

——就是这句！上次我就是在这里脑子打架的：第 6 页说从 0 开始，这里说从 1 开始，两条规则没人告诉我什么时候用哪条。现在口诀一句话分好了：**定义的时候电脑从 0 数**（天生的），**做题的时候我从 1 用**（故意的），0 号柜空着占位。我读了两遍，背下来了："定义从 0 数，比赛从 1 用"。押韵，好记，像顺口溜。上次那种"两条规则打架"的感觉没有了。

- 新授示例 9 行（行 173-181）✅
- 全局 `int a[105]` 在 main 外面（行 175），和第 8 页对得上 ✅

## 第 11 页：检查点：循环到哪？（修复验证关键题）

**闭卷**：题目白纸黑字写着"**下标从 1 开始**读入 n 个数"——口诀：比赛从 1 用！`for (int i = 1; i <= n; i++)`，选 A。**这次我没有犹豫，没有回上去翻卡片，直接选的。** 上次我先选了 C（`i = 0` 开始）又回看提示才改对，这次 0 开始的念头根本没冒出来——因为口诀把"0"归给"定义"、把"1"归给"比赛"，各占各的位置，不打架了。
**对答案**：A ✅ 独立通过（上次这题是 🟡）。

## 第 12 页：卡片：查找最大值（重点复测页）

打擂台 🥊，上次全场最佳，这次还是喜欢。重点看 demo（行 213）：

`int a[6] = {0, 90, 85, 70, 48, 91}, maxv = a[1];  // 0 号柜空着占位，成绩放 1~5 号柜`

——注释！"**0 号柜空着占位，成绩放 1~5 号柜**"。上次这个 `{0, 90, ...}` 开头塞个 0，没人说是干嘛的，我自己猜的"配合从 1 开始用"。现在注释直接写明白了，而且和第 10 页口诀里"0 号柜空着占位"是同一句话，前后对上了。不用猜的感觉真好。

- 新授示例 9 行（行 210-218）✅

## 第 13 页：卡片：最大值的位置

小本本 `pos` 记位置，换擂主顺手记号码。demo（行 237）同样有"0 号柜空着占位"注释，和第 12 页一致。复习，顺。

- 新授示例 9 行（行 234-242）✅

## 第 14 页：检查点：哪个位置？

**闭卷**：两个一样大的最大值，输出哪个位置？……"想一想"题，我的死穴，上次我凭手感猜 C 错了。这次我盯着 `if (a[i] > maxv)` 看了 10 秒（真的数了，10 秒）：严格大于，一样大就**不换**擂主，先来的坐稳——第一个，选 A。坦白说，上次看答案懂透了的印象还在，不全是重新推的；但这次我能把道理自己讲出来（"严格大于，一样大不换"），不是背答案字母。
**对答案**：A ✅（按"闭卷写对且能讲清道理"记 ✅，比上次的 🟡 进一步；诚实标注：有复习记忆加成）。

## 第 15 页：挑战 1：数组输入输出

**闭卷**：骨架 + 全局 `int a[105]` + 两个 for（读入、输出），`i <= n`。上次我差点写成 `i < n`，这次写的时候手又往 `<` 上滑了一下，自己刹住了（题后"容易错"卡片说漏掉最后一个数，说的就是我，我记得）。写完自查一遍边界。
**对答案**：与老师的一致（行 275-283）。✅ 独立通过，连续第二次。

题后"考察什么/解题思路/容易错"卡片（行 287-313）复习一遍，"循环写成 `i < n` 漏掉最后一个数"——还是我，但我现在会自查了。

## 第 16 页：挑战 2：最大值的位置

**闭卷**：读入循环没问题。然后写擂台——`int maxv = ...`，我的肌肉记忆又想说 0！停了 10 秒，想起上次题后卡片点我名（行 360："maxv 写成 0，全是负数就找不到答案"），改成 `maxv = a[1], pos = 1`。循环里 `if (a[i] > maxv) { maxv = a[i]; pos = i; }`——这次 pos 没忘，写完我还特意回头检查了一遍"pos 写了吗"。**没翻卡片，自己想起来的。**
**对答案**：与老师的一致（行 329-340）。✅ 独立通过（上次 🟡）。得承认：是记住"容易错"卡片救的我，不是天赋。

**投诉维持**：这题示例从 `#include` 到 `}` 还是 **12 行**（行 329-340），一口气读入+擂台+记位置。虽然这次我会做了，但第一次学的人看 12 行还是会眼睛累——我上次就是抄这段抄漏了 `pos = i`。

## 第 17 页：小贴士

开大点、全局自动全 0。复习，扫一眼过。

## 第 18 页：课后挑战（5 道，用快照闭卷做）

徽章！上次就栽在这页。这次一道一道来：

**题 1 人数统计**：闭卷。5 个分数段计数，`int cnt[5] = {0};` 加分支分箱子。上次我把 90 错分到 80~89 段，这次写到 90~100 那段我停了一下："90 归哪段？——90~100 段的**下限就是 90**，`s >= 90`。"自己跟自己确认了一遍才写。写完对样例：7 个数 → 1、2、0、2、2，一次对。✅ 独立通过（上次 🟡，分段边界这次没滑）。

**题 2 找数组的最大数**：闭卷。`maxv = a[1]` 起手（不写 0！），逐个挑战。样例 `2 1 5 3 4` → 5。一次写对。✅ 连续第二次独立通过，这题现在是我的拿手题，小柯你等着。

**题 3 找最小数的位置**：闭卷。找最小，`minv = a[1], pos = 1`，判断写 `<` 还是 `<=`？上次我顺手写 `<=` 靠样例救回来，这次我想到第 14 页检查点的道理：题目要**第一个**出现的，一样小不换擂主，所以严格 `<`。样例 `2 1 3 4 1` → 2，两个 1，第一个在 2 号位，对。✅ 独立通过（上次 🟡）——这次是靠想通的，不是靠样例救的。

**题 4 奇数位偶数位和与差**：闭卷。看到题目我先冷笑了一声："又是你。"上次我把"奇数**位**"看成"奇数"，算出 0 懵了 30 秒。这次我**先读括号**（"注意：不是奇数和偶数的和，是奇数位和偶数位的和"），再动笔：第 1、3、5 个加一拨，第 2、4 个加一拨，按下标 `i % 2 == 1` 分。输出三行：奇位和、偶位和、大减小。样例 4 6 8 10 12 → 24、16、8，对。✅ 独立通过（上次 🟡）——被坑过一次，这次括号我逐字读的。

**题 5 找找谁的身高超过全家的平均身高（新题！）**：闭卷。读题：全家 n 口人，输入 n 个身高，先输出平均身高（保留一位小数），再输出超过平均身高的人。思路我顺了一遍：读 n → 循环读进数组 `h[i]` → 顺手累加 `sum` → 平均 `ave = (double)sum / n`（转小数，GESP1 老知识）→ 再遍历一遍，`h[i] > ave` 就输出。**全部步骤都在课内**：数组读入（挑战 1）、遍历求和（第 10 页）、平均转 double（GESP1-03）、按条件输出（循环套 if）。上次那个"两个擂主"的鬼题没有了，这个我能做！

写代码。第一遍输出我写的是：直接 `cout << fixed << setprecision(1) << ave << endl;`，然后 `if (h[i] > ave) cout << h[i] << " ";`。对样例——`AVE=163.9`？`1:175 3:172 5:178`？啊？**平均身高前面要加 `AVE=`，超过的人要带位置 `1:`、`3:`、`5:`！**题面只说"第一行为全家的平均身高""第二行有若干个数，为超过平均身高的人的身高厘米数"（快照行 117-121），一个字没提 `AVE=` 前缀，也没提要带位置编号！我又回头看样例才改过来：`cout << "AVE=" << ...`，`cout << i << ":" << h[i] << " ";`。改完对样例：163.9，第 1、3、5 个超过，输出 `1:175 3:172 5:178`，对。

🟡 半独立——**核心思路全程独立、没有卡**，但输出格式被样例救了一次。跟上次的 ❌ 放弃天壤之别：上次是"根本不会做"，这次是"会做，就是题面没把格式写全，害我返工一次"。有点不爽，但徽章到手了。

---

## 客观测量

- **最长中文行**：全课每页最长连续中文字符数均 ≤ 80。最长为第 4 页 ⚠️ 警告段（行 50），约 63 字（不含代码/符号），达标但贴边；其余各页 20~50 字。**全部达标**。
- **未解释新术语清单**：
  - "遍历"（第 10 页，行 164）——已解释 ✅
  - "数组大小必须固定"（第 4 页，行 50）——已解释 ✅
  - "下标"（第 6 页，行 85）——已解释 ✅
  - "口诀/占位"（第 10 页，行 168）——新增内容，自带解释（"柜子编号天生从 0 开始；做题时故意从 1 号柜开始用，0 号柜空着占位"）✅
  - **"越界"（第 6 页，行 89）——⚠️ 仍只有后果（"不报错、读到乱糟糟的数据"）没有词义解释（超出柜子编号范围），v1 提过，未改。这次靠第 10 页口诀我才自己脑补出词义。**
- **示例代码行数**（小明上限 ≤10 行）：
  - 第 4 页 9 行、第 6 页 8 行、第 8 页 7 行、第 10 页 9 行、第 12 页 9 行、第 13 页 9 行、挑战 1 示例 9 行——**全部达标**
  - **挑战 2 示例 12 行（行 329-340），仍超标 ❌（v1 提过，未改）**

## 独立完成测试（三步法）

| 题目 | 闭卷结果 | 对答案差异 | 判定 |
|------|----------|-----------|------|
| checkpoint1 哪种对？ | B，秒选 | 0 处 | ✅ |
| checkpoint2 第 5 个？ | 口诀"定义从 0 数"→ B，不晃 | 0 处 | ✅ |
| checkpoint3 总和？ | B（5），心算 | 0 处 | ✅ |
| checkpoint4 循环到哪？ | 口诀"比赛从 1 用"→ A，**没回看卡片**（上次先选 C 回看改 A） | 0 处 | ✅ |
| checkpoint5 哪个位置？ | 盯 `>` 10 秒，"严格大于一样大不换"→ A（有复习记忆加成，能自讲道理） | 0 处 | ✅ |
| 挑战1 输入输出 | 写对，`i<n` 手滑自查刹住 | 0 处 | ✅ |
| 挑战2 最大值位置 | 肌肉记忆想说 `maxv=0`，自己想起"容易错"卡片改 `a[1]`；pos 没忘；**没翻卡片** | 0 处 | ✅ |
| 课后题1 人数统计 | 90 分段边界自问自答确认后写对 | 0 处 | ✅ |
| 课后题2 找最大数 | 一次写对 | 0 处 | ✅ |
| 课后题3 最小数位置 | 用"严格 < 保第一个"的道理直接写 `<`，不靠样例救 | 0 处 | ✅ |
| 课后题4 奇偶位和差 | 先逐字读括号再动笔，思路对 | 0 处 | ✅ |
| 课后题5 平均身高（新题） | 思路全独立（读入→求和→平均→按条件输出），但 `AVE=` 前缀和 `位置:身高` 格式题面未写，对样例才发现，返工 1 次 | 1 处（输出格式，看样例改对） | 🟡 |

- ✅ 11 题，🟡 1 题，❌ 0 题
- **完成度 = (11×1 + 1×0.5) / 12 = 11.5 / 12 = 0.958 → 保留 1 位小数 = 1.0**

## 卡点日志

- [课后题 5] 卡住：输出和样例对不上（缺 `AVE=`、缺位置编号），愣住约 20 秒，嘀咕"题面也没说啊" → 回看样例改格式，约 1 分钟解决。**没有触发放弃**（上次旧题 5 改 3 遍不对直接想关页面）。
- 准卡点（未真正卡住，诚实记录）：挑战 2 写 `maxv` 时停了约 10 秒回忆"容易错"卡片，自己想起 `a[1]` 起手，未翻页；检查点 5 盯题 10 秒才作答，未看答案。

## 修复验证（逐条结论）

**① 新题 5（"找找谁的身高超过全家的平均身高"）还会不会做不出来？——修复有效。**
所需知识（数组读入、遍历、求和、平均转 double、按位置输出）全部在课内（第 10 页遍历、挑战 1 读入、GESP1-03 保留小数），我核心思路一遍通，没有任何"课里没教"的步骤。与旧题 5（双擂主，❌ 放弃）相比天壤之别。残留瑕疵：题面文字没写 `AVE=` 前缀和 `位置:身高` 格式（快照行 117-121 vs 样例行 130-133），我返工 1 次记 🟡——这是题面表述问题，不是超纲问题。

**② 下标口诀（"定义从 0 数，比赛从 1 用"，行 168）能不能解决上次检查点 4 选错的问题？——修复有效，这是本次复测最大改善。**
上次检查点 4 我先选 C（0 开始）再回看卡片改 A（🟡）；这次口诀把"0"归定义、"1"归比赛，我**直接选 A、没有犹豫、没有翻页**（✅）。检查点 2（第 5 个 = `a[4]`）也顺手受益，上次是悬崖边拉回，这次稳稳的。口诀还顺带解释了 `{0, ...}` 占位（"0 号柜空着占位"），和第 12/13 页注释呼应。

**③ 打擂台 demo 的 `{0, 90, ...}` 占位注释（行 213、237）有没有用？——修复有效。**
上次"第 0 格空着不用"是我**自己猜的**；这次注释"0 号柜空着占位，成绩放 1~5 号柜"直接写明，且与第 10 页口诀用词一致（"0 号柜空着占位"），前后形成呼应，读 demo 时不用再停下来猜。第 12、13 页两张卡片的读图成本明显下降。

## 评分

- **理解率：75**（证据：全部新授点——定义、下标、初始化、遍历、擂台、记位置——本次当堂能独立使用，5 道 checkpoint 全 ✅ 且能自讲道理；但"越界"一词仍半懂（行 89 未改，靠口诀脑补词义），达不到"全懂"，够不上 100；比上次同档 75 的成色更实——上次的 0/1 混淆这个主要扣分点已消除）
- **完成度：1.0**（按公式 (11×1 + 1×0.5)/12 = 0.958，保留 1 位小数为 1.0）
- **是否通过：是**（小明线 = 理解率 ≥75 且完成度 ≥0.75 且愿意继续：75 ≥ 75 ✓，1.0 ≥ 0.75 ✓，愿意继续 = 是 ✓。**v1 三项里两项不过，本次三项全过。**）

## 必须修改的问题

1. **新题 5 题面文字与样例输出格式不一致。** 题面（快照行 117-121）只说"第一行为全家的平均身高（保留一位小数）；第二行有若干个数，为超过平均身高的人的身高厘米数"，但样例输出（行 130-133）是 `AVE=163.9` 和 `1:175 3:172 5:178`——`AVE=` 前缀和"位置:身高"格式**只出现在样例里，题面一个字没提**。我核心思路全对，却因格式返工 1 次（🟡）。建议：题面输出说明改为"第一行输出 `AVE=平均身高`（保留一位小数）；第二行按 `位置:身高` 的格式输出超过平均身高的人，空格分隔"。
2. **挑战 2 示例代码仍 12 行（行 329-340），超 10 行上限，v1 提过未改。** 读入+擂台+记位置三件事塞一段，第一次学的人（比如上次的我）容易抄漏 `pos = i`。建议：读入部分标注"和挑战 1 一样"，只新授擂台 4 行核心（`maxv = a[1], pos = 1` + 循环判断）。
3. **"越界"仍只有后果没有词义解释（行 89），v1 提过未改。** "电脑不会报错，但会读到乱糟糟的数据"让人更慌。建议补一句："越界就是超出柜子的编号范围——比如 `a[5]` 这格根本不存在。电脑不报错，所以下标要自己数清楚。"

（诚实说明：本次只找到 3 个必须修改的问题。上次的 3 个主问题——题 5 超纲、0/1 规则打架、占位无说明——均已修复并验证有效，不再重复列出。）

## 愿意继续吗

**是。** 5 道课后题全过（题 5 虽然返工一次但是我自己改对的），徽章到手！上次是摔笔走的，这次是哼着歌走的。第 2 课，我来了——小柯，这次题 5 你肯定也行，但我的徽章不比你晚！

## 一句话总结

"口诀一背，0 和 1 不打架了；新题 5 我会做了！就是题面没说要写 `AVE=`，害我返工一次——下次题面能不能把话说全？"

## 评分自检

- [x] 每个分数旁边都有 1 行证据？（理解率、完成度均附证据，含"越界"行 89 锚点）
- [x] 完成度是按公式算的，不是估的？（(11×1 + 1×0.5)/12 = 0.958 → 1.0）
- [x] 理解率是 0/25/50/75/100 之一？（75）
- [x] 至少列了 3 个具体问题？（3 个，均带行号证据；已如实说明上次 3 个主问题修复有效不重复列）
- [x] 完成"闭卷"尝试了，还是开卷？开卷是否已声明？（课件 demo 无法隐去，已在报告开头显著声明开卷；三步法按闭眼模拟逐题记录，含"复习记忆加成"的诚实标注）
- [x] 有没有把"看着示例能跑"误当成"理解了"？（没有——题 5 输出格式看样例改对，记 🟡 不记 ✅；checkpoint 5 的复习加成已如实披露）
- [x] 小明特有项：**愿意继续学下一课吗？——是**（5 题全过 + 徽章获得 + 无放弃级卡点）
