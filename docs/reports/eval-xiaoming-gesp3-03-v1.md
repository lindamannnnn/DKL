# 小明评测报告：GESP3-03《数组计数法》（v1）

## 身份与声明

- **我是谁**：小明，11 岁，五年级，零基础起步，容易走神，做题靠感觉。
- **基线身份声明**：我学完了 GESP1 + GESP2 全部课程（以 `gesp1-graduate.md` + `gesp2-graduate.md` 为准，但有些细节我记不全），外加 GESP3-01《一维数组》和 GESP3-02《数组增删》。这是第 3 课。
- **开卷声明**：示例代码和答案就印在题旁边，课件没法帮我藏起来。评估者按三步法让我先闭卷再对答案，但答案物理可见，我的独立完成结论**可能高估**，特此声明。

---

## 逐页体验记录

### 故事：选班长
选班长投票，还行吧，比"成绩怎么存"好玩一点。没有动画，我眼睛飘了一下。

### 回顾
"遍历一个个比较慢"——哦。快翻过去。

### 卡片：下标当桶用
`cnt[a[i]]++`，下标当桶……大概懂了，就是票上写几就往几号桶扔嘛。这有什么难的。代码 9 行，我扫了一眼，没逐行看。

### 检查点：桶里几票
c[a[i]]++，a 是 2 1 2 1……1 出现两次，选 B。对了 ✅。（嘿嘿，我数感还行。）

### 卡片：桶要先清空
"要清 0"……"写在 main 外面自动清 0"……后面还有一句什么 100、101 的，字有点多，我扫过去没细读。**（走神点：漏看了"值最大 100 要开 cnt[101]"）**

### 检查点：数组开多大
1~100 的票？那就 `int cnt[100]` 呗，100 个数开 100，选 A！……答案是 B？？为啥啊？（翻回去重读卡片，哦，下标 0~100 一共 101 个桶。刚才那段我根本没读完。）❌

### 卡片：谁是票王
打擂台我熟！扫一遍桶，谁球多谁当冠军。代码没细看，反正就是 for 套 if。

### 检查点：票王是几号
2 1 2 3 2，2 有 3 票，选 B ✅。

### 卡片：桶排序
倒出来自动排好队？`while (cnt[v]--)` 这行什么鬼，cnt 减着减着就没了？不想细看，反正结论我记住了：从小到大倒桶 = 排好序。

### 检查点：倒出来啥样
排好序的，选 B `1 1 3 4 5` ✅。

### 挑战 1：统计票数
闭卷写：我在 main 里面写了 `int cnt[100];` 没初始化（完全忘了清 0 那回事），然后循环 cnt[x]++，扫 1~100 全输出了。对答案：错 2 处——没清 0（还可能越界）、没判断 `cnt[v] > 0`。看了一遍题解改对 → 🟡。

### 挑战 2：票王是谁
闭卷写：统计 + 打擂台，判断我顺手写了 `>=`（习惯），输出只写了 ans 没写次数。对样例：`1 2 3 2 1 2 3` 里 2 出现 3 次是唯一最多的，`>=` 结果一样，样例居然也能过？！再对题解：题目要输出"数 和 次数"两个值，我漏了 maxC。看了一次题解改对 → 🟡。**注意：这题样例没有并列，我写 `>=` 根本暴露不出来，我还以为自己对了。**

### 小贴士
三条小字，扫了一眼。第二条"值太大不能用"后来救了我一命（见课后题 5）。

### 课后挑战
见下表。

---

## 客观测量

- **最长中文行**：全课每页最长连续中文字符数最大 18 字（挑战 1，脚本逐页测量），≤80 → **全部达标**。
- **未解释新术语**："频次"（挑战 1 考察点，没解释，我当它是"次数"蒙的）；"垃圾值"（挑战 1 易错点，没解释，不想猜）。
- **示例代码行数**（我的上限是 10 行）：新授 3 个 demo 各 9 行 → 达标；**挑战 2 题解 12 行，超标 2 行**；挑战 1 题解 10 行，压线。

## 独立完成测试（三步法）

| 题目 | 闭卷结果 | 对答案差异 | 判定 |
|------|----------|-----------|------|
| 检查点1 桶里几票 | 数出 c[1]=2 | 0 处 | ✅ |
| 检查点2 数组开多大 | 选了 cnt[100]（没读完卡片） | 1 处 | ❌ |
| 检查点3 票王是几号 | 2 号 3 票 | 0 处 | ✅ |
| 检查点4 倒出来啥样 | 1 1 3 4 5 | 0 处 | ✅ |
| 挑战1 统计票数 | 没清 0、没过滤 cnt[v]>0 | 2 处，看 1 次题解改对 | 🟡 |
| 挑战2 票王是谁 | 写了 `>=`、漏输出次数 | 1 处格式 + 1 处隐患，看 1 次题解改对 | 🟡 |
| 课后题1 求n个数中每个数出现的次数 | 照搬挑战 1 改好的写法，cnt[105] + 过滤 | 0 处 | ✅ |
| 课后题2 出现次数最多的数 | 同挑战 2，只输出数字（题目保证唯一最多） | 0 处 | ✅ |
| 课后题3 数字出现次数 | **把第一个数 1 当成 n**，程序读 1 个数就结束了，输出乱七八糟 | 思路错误，题面"有 50 个数"我没看到 | ❌ |
| 课后题4 统计数字出现次数（找 M） | 数组 + 计数做出来了，但第一次位置从 0 开始数、忘了"未找到输出 0" | 重读题面改对 1 次 | 🟡 |
| 课后题5 统计每个数出现的次数 | 值是 int 范围，桶开不下，直接放弃（小贴士自己都说不能用） | — | ❌（超纲） |

- **完成度 = (✅×5 + 🟡×0.5×3) ÷ 11 = 6.5 ÷ 11 ≈ 0.6**

## 卡点日志

- [卡片：桶要先清空] 走神：文字超过 3 行我就跳了，漏掉"开 101"的规则 → 直接答错检查点 2。
- [卡片：桶排序] 卡住：`while (cnt[v]--)` 看不懂，约 20 秒 → 选择跳过，只记结论。
- [课后题 3] 卡住：程序读 1 个数就结束，输出全错，搞不懂为什么，烦躁约 90 秒，抱怨"这题有问题吧" → 想退出。最后重读题面才发现没有 n。
- [课后题 5] 放弃：桶开 21 亿？电脑要炸了。这题没法做，退出。

## 评分

- **理解率：50**（证据：4 个检查点对 3 个说明 `cnt[x]++` 的具体数数我懂；但桶要清 0、桶大小按值域开、只输出出现过的数、`>` 与 `>=` 的区别，这 4 个细节我全都需要对答案才发现——懂一半）
- **完成度：0.6**（按公式 6.5/11）
- **是否通过：❌ 不通过**（理解率 50 < 75，完成度 0.6 < 0.75）
- **愿意继续吗：✅ 愿意**（桶排序"超快 ⚡"像作弊码一样爽，比上一课逆序对好玩；而且小柯要是过了我没过，我不服。但第 5 题那种根本做不了的题别再放了，烦。）

## 课后题超纲判定（逐题核对"GESP1/2 基线 + GESP3-01/02 已教 + 本课已教"）

| 题 | 所需知识 | 判定 |
|----|----------|------|
| 1 求n个数中每个数出现的次数 | cnt[x]++ + 过滤输出（本课） | 不超纲 |
| 2 出现次数最多的数 | 计数 + 打擂台（本课） | 不超纲 |
| 3 数字出现次数 | 固定 50 次循环 + 计数；知识不超纲，但**输入无 n 与全课"第一行 n"约定冲突**，对不读题的学生是灾难 | 不超纲，**强烈建议修订题面或替换** |
| 4 统计数字出现次数（找 M） | 遍历 + 计数 + 记首次位置（GESP3-01 + GESP1-13） | 不超纲 |
| 5 统计每个数出现的次数 | int 全范围值 → 计数数组开不下；需 map/排序，**未教**（来源标签自证"容器 map"） | **❌ 超纲** |

**超纲处理建议**：不修改课件/题库，建议替换第 5 题。候选（SQL 核实 active）：
- 首选 `03c23bb2-1ce9-496d-83ef-e70818c32fdf`（COUNT：页码数字 0~9 计数，cnt[10] + 拆位，全教过）
- 保底 `f229a249-9e0d-459f-a4cd-13f68f60f675`（数组元素的查找，遍历找位置）

## 至少 3 个必须修改的问题

1. **课后题 5（cc437f63）超纲**：int 范围值没法开桶，需要没教过的 map/排序。我这种不爱认输的人都直接放弃了，必须换题（候选见上）。
2. **课后题 3（a62b0579）输入格式是暗坑**：全课+前两课所有程序题都是"第一行 n"，这题没有 n、固定 50 个数。我直接把第一个数当 n，程序读 1 个数就结束了，根本想不到要重读题面。建议题面第一行加粗写"注意：本题没有 n，固定 50 个数"，或换成带 n 的同型题。
3. **挑战 2 样例放跑了 `>=` 错误**：样例 `1 2 3 2 1 2 3` 里最多的数唯一，写 `>=` 和 `>` 结果一样，错误写法能蒙混过关，我还以为自己对了。建议样例改成有并列的数据（如 `1 2 1 2`），让 `>=` 当场露馅。
4. **「桶要先清空」卡片的关键规则埋在一句话里**："值最大 100 就开 cnt[101]"是检查点 2 的考点，但它夹在卡片第二行，我扫过去就漏了。建议把"开多大 = 最大值 + 1"单独放大或做成口诀。
5. **挑战 2 题解 12 行，超我 10 行上限**：对答案时看到 12 行代码我直接不想逐行对了。

## 一句话总结

"投桶计数我会，但是什么 101、清 0 那些规矩真烦人！第 5 题根本不是给人做的。不过桶排序唰一下就排好序，这个帅，我还想继续玩。"

## 提交前自检

- [x] 每个分数旁边都有 1 行证据
- [x] 完成度按公式 (✅×1+🟡×0.5)/总题数 计算
- [x] 理解率取 0/25/50/75/100 之一（50）
- [x] 列了 5 个具体问题（≥3）
- [x] 完成闭卷尝试，开卷风险已在开头声明
- [x] 没有把"看着示例能跑"当成"理解了"（挑战 1/2 如实记 🟡，`>=` 蒙混如实披露）
- [x] 包含"愿意继续吗"判定（✅ 愿意）
