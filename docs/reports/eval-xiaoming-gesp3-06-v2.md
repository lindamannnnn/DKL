# 小明评测：GESP3-06 字符数组（v2）

> 本次为**盲测**（未读任何旧报告）。课件无法折叠示例代码，故 checkpoint 后的"挑战"题属于开卷可看讲解，但所有 checkpoint 与 5 道课后题均按**闭卷三步法**执行（先遮住示例自己做，再对答案）。特此声明。

## 测试课程：GESP3-06 字符数组

### 逐页第一反应（按 `## ` 分页）

- **P1 故事**：名字存不进 int，哈哈这个我懂，字母又不是数字。一排柜子装字母，行吧，继续。
- **P2 回顾**：int 数组我会，第 01 课学过，跳过跳过。
- **P3 char 密码表**：ASCII 我早就会了！GESP2-04 就背过 `'0'=48`、`'A'=65`、`'a'=97`。这页 3 秒翻完。代码 9 行，不用看。
- **CP1**：'B' 是 66！选 B。✅ 秒答，太简单。
- **P4 大小写差 32**：这个 GESP1 就学过了，大写+32 变小写。又复习一遍，有点无聊，走神了 10 秒。
- **CP2**：'D'+32='d'，选 B。✅
- **P5 字符数组**：哎？新东西。`char a[] = "mike"` 后面还有个看不见的小旗子 `'\0'`，4 个字母占 5 格。这个比喻我喜欢，旗子=终点。
- **CP3**：`"hello"` 占几格？5！……等等，上一页刚说 mike 4 个字母占 5 格，那 hello 5 个字母就是 6 格。选 C。🟡（首答 B 错，重读上一页才改对——我果然又抢答了）
- **P6 一格一格装**：亲手装要记得放旗子，忘了会读出乱码👻。行。
- **CP4**：最后一格放啥？`'0'` 吧……不对不对，是小旗子 `'\0'`，`'0'` 是数字零的字符。选 B。🟡（首答 A 错，这俩长得太像了！）
- **P7 读入字符串**：`cin >> s` 读到空格就停，`cin.getline(s, 100)` 读整行。懂了，一个单词用 cin，一句话用 getline。
- **P8 常用小助手**：⚠️ 一下蹦出来 **4 个新函数**（strlen、strcat、strcpy、strcmp）！我最多记住 3 个。strlen 数长度我记住了，strcmp 比大小记住一半（负数是啥意思来着），strcat 和 strcpy 看完就混——一个接尾巴一个抄写，哪个是哪个？这页我来回看了两遍还是糊。
- **CP5**：`strlen("apple")` = 5，不数旗子。选 B。✅
- **P9 sizeof 数格子**（新补内容）：这个对比好！strlen 数货物、sizeof 数格子，`char a[10] = "hi"` 货物 2 件格子 10 个。例子直接给答案，我记住了。
- **CP6**：`sizeof(a)` = 10，选 C。✅（上一页例题就是这题，白送）
- **P10 亲手数长度**：while 循环看到 `'\0'` 就停，我会 while，能看懂。代码 8 行，行。
- **CP7**：看到 `'\0'` 停，选 A。✅（这次没选错，被 CP4 教训过了）
- **P11 大小写变身**：⚠️ 又来！tolower、toupper 加**五个侦探**（isalpha、isdigit、isupper、islower、isspace），一共 7 个新名字挤一页！我只记住了 tolower/toupper 和 isdigit，isspace 是干嘛的？isalpha 怎么拼？demo 里只用了 isupper，其他侦探就露个脸，我肯定记不住。
- **CP8**：tolower('B')='b'，选 B。✅
- **P12 字母绕圈走**（新补内容）：恺撒密码！y 挪 3 格变 b，绕圈好玩。但那个公式 `(c - 'a' + k) % 26 + 'a'` 好长，看着像咒语。讲解分三步我**看得懂**，让我自己写绝对写不出来。
- **CP9**：'z' 挪 1 格绕回 'a'，选 A。✅（概念题，不考公式）
- **P13 挑战 1**：统计大写/小写/数字。代码 13 行有点长，但下面有分步讲解，照着能看懂。三个计数器我会（GESP1 累加桶里丢石头）。
- **P14 挑战 2**：strcmp 比大小，负数 a 小。讲解里"直接用 a < b 比的是地址不是内容"——地址是啥？没学过，这句话直接跳过。
- **P15 小贴士**：`char a[5] = "hello"` 是错的要 6 格，哈哈幸好 CP3 我改对了。

### 客观测量

- 最长中文行：各页均在 80 字以内（最长为 P11 侦探列举行约 40 字），**达标**。
- 未解释新术语：
  - `地址`（P14 讲解，"比的是地址不是内容"，无解释——指针/地址本来就没学过）
  - `strcat` / `strcpy`（P8 只有一句话定义，无 demo 区分练习，10 分钟后必混）
  - `isspace` / `isalpha`（P11 只有半句话定义，全课无使用示例）
- 示例代码行数：挑战 1 demo 13 行（超小明 10 行上限，但有分步讲解兜底）；其余 demo ≤ 10 行。

### 独立完成测试（三步法）

| 题目 | 闭卷结果 | 对答案差异 | 判定 |
|------|----------|-----------|------|
| CP1 'B'的ASCII | 秒答 66 | 0 处 | ✅ |
| CP2 'D'+32 | 秒答 'd' | 0 处 | ✅ |
| CP3 "hello"格子数 | 首答 5（错） | 重读 P5 后改 6 | 🟡 |
| CP4 最后一格放啥 | 首答 '0'（错） | 重读 P6 后改 '\0' | 🟡 |
| CP5 strlen("apple") | 5 | 0 处 | ✅ |
| CP6 sizeof | 10 | 0 处 | ✅ |
| CP7 数到哪儿停 | '\0' | 0 处 | ✅ |
| CP8 tolower('B') | 'b' | 0 处 | ✅ |
| CP9 'z'挪1格 | 'a' | 0 处 | ✅ |
| 课后1 统计大写字母（以.结束） | 卡在"怎么停在小数点"，想不起该边读边判还是读整行再判 | 看挑战1的循环骨架后写出 `while(s[i]!='.')` + isupper 计数 | 🟡 |
| 课后2 大小写转换 | 独立写出：cin>>s，循环 `s[i]!='\0'`，大写+32小写-32（用我 GESP1 的老办法，没用 tolower） | 与标答思路一致 | ✅ |
| 课后3 统计字母数字空格其他 | 卡在"空格怎么判"——isspace 我没记住，最后用 `s[i]==' '` 硬判；骨架偷看了挑战1 | 差 1 处（需看示例回忆计数器骨架） | 🟡 |
| 课后4 倒置输出字符串 | 独立写出：strlen 求长度，`for(i=len-1;i>=0;i--) cout<<s[i]`（第01课逆序输出+i--倒着走） | 0 处 | ✅ |
| 课后5 比较字符串 | 卡在"怎么拿到差值"——strcmp 不给我差值数字啊？手动循环 `while(s1[i]==s2[i] && s1[i]) i++;` 这个写法我自己没想到 | 看挑战2"遇到第一对不同就分胜负"的讲解后才写出 `cout<<s1[i]-s2[i]` | 🟡 |

- **完成度 = (✅×11 + 🟡×5×0.5) / 16 = (11 + 2.5) / 16 = 13.5/16 = 0.8**
  （checkpoint 9 题：✅7 🟡2；课后题 5 题：✅2 🟡3；合计 16 题）

### 卡点日志

- [P8] 卡住：4 个函数名挤在一页，strcat/strcpy 反复看仍混淆，持续约 40 秒 → 触发跳过（心想"用到再说"）
- [P11] 卡住：7 个函数名一页，isspace/isalpha 拼写都记不住，约 30 秒 → 触发走神
- [课后5] 卡住：不知道手动逐字符比较的循环怎么收尾，约 90 秒无进展 → 看挑战 2 讲解

### 评分

- 理解率：**75**（证据：\0、strlen、sizeof、getline、恺撒概念都能复述且 CP 答对；但 P8 的 strcat/strcpy、P11 的 isspace/isalpha 只露脸没学会，P12 恺撒公式看得懂写不出——扣一档不到 100）
- 完成度：**0.8**（按公式 13.5/16）
- 是否通过：**是**（75≥75，0.8≥0.75，愿意继续打勾——今天没摔键盘，恺撒密码还挺酷）

### 至少 3 个必须修改的问题

1. **P8「常用小助手」一页塞 4 个新函数，超小孩一次记 3 个的上限**（证据：strcat/strcpy 全课没有任何 demo 或 checkpoint 区分练习，我看完即混；建议 strlen/strcmp 一组、strcat/strcpy 拆到下一页并各配 1 个 checkpoint）
2. **P11 一页塞 7 个函数名（2 变身 + 5 侦探）**（证据：isspace、isalpha 全课零示例零练习，课后 3 需要判空格时我根本想不起 isspace，只能用 `s[i]==' '`；建议侦探拆 2 页，isspace 配一个 demo）
3. **课后 5「比较字符串」需要"手动逐字符比较并输出差值"的循环写法，但全课只教了 strcmp，从没演示过手动比较的循环长什么样**（证据：我闭卷卡在循环收尾 90 秒；挑战 2 讲解里虽有"遇到第一对不同就分胜负"的文字，但没有对应代码示例）
4. **P14 讲解出现"地址"一词无解释**（证据：指针/地址是后面才学的概念，这句话对我就是乱码，直接跳过）

### 一句话总结

"小旗子 `\0` 我学会了，恺撒密码好酷！但是那两页一口气塞四五个英文函数名的是谁排的版？我脑袋装不下啦！"
