# 测评报告：GESP3-07 string使用1（小明 · 重置后重学 v3）

> 测评身份：小明（11岁五年级），GESP2 毕业基线 + 已学 GESP3-01~06（含字符数组）。
> 本课是 C++ `string` 类入门，方法密集（共 13+ 个接口）。独立作答基于快照 `problems-gesp3-07.md`，闭卷三步法。

## 学习日记（逐页体验，第一人称）

- **故事页**：弹性魔法袋 string 🎒，想装多少装多少，自动变大变小。开头有袋子图，我眼睛没飘——比字符数组省心，我喜欢。
- **回顾页**：对比字符数组的 `strcat`/`strcmp`/`\0` 麻烦，引出 string。✅
- **卡片·string来啦**：`+` 拼接、`==`/`<` 比较，不用管 `\0`。小明：太爽了，比 char 数组方便一百倍。✅
- **checkpoint1（拼起来是啥）**：`s+="cd"`→"abcd" 选 C ✅。
- **卡片·更多初始化**：`string(5,'a')`→"aaaaa"，`length()`/`size()` 双胞胎。✅
- **checkpoint2（造出什么）**：`string(3,'x')`→"xxx" 选 A ✅。
- **卡片·读出整行**：`getline(cin,s)` 整行、`s.size()`、`s[i]` 下标0。✅
- **checkpoint3（长度是几）**："hi c++" size=6（含空格）选 B ✅——没把空格漏掉，棒。
- **卡片·两种读整行**：`cin.getline(s,100)`（字符数组）vs `getline(cin,s)`（string），收件人不同。小明：这俩容易混，我特地念了遍。
- **卡片·find找一找**：`s.find("abc")` 返回下标，找不到 `string::npos`。✅
- **checkpoint4（找到哪儿）**：find("cd") in "abcde"=2 选 B ✅。
- **卡片·replace替换**：`s.replace(起点,个数,"新")` 袋子自动伸缩。✅
- **checkpoint5（换成什么）**：replace(0,5,"hi")→"hi world" 选 A ✅。
- **卡片·清空与查空**：`s.clear()` 倒空、`s.empty()` 空了答 true。✅
- **checkpoint6（袋子空了吗）**：clear 后 empty=true 选 A ✅。
- **卡片·塞入与删除**：`insert(位置,"内容")`、`erase(起点,个数)`，口诀"insert 给位置，erase 给起点和个数"。小明：方法开始变多，我眼皮稍微沉了一下。
- **checkpoint7（删掉剩啥）**：erase(1,2) "abcd"→"ad" 选 A ✅。
- **卡片·尾巴上加料**：`s.append(...)` 接尾巴，还能 `append(s2,pos,len)`。小明：到这我开始想"怎么又来一个"，有点走神，2 分钟没动手觉得无聊。
- **卡片·数字变身串**：`to_string(123)`、`stoi("456")`、`stod("3.14")`。✅ 数字字符串互转我会用。
- **checkpoint8（变身加加看）**：stoi("38")+2=40 选 A ✅。
- **卡片·大小写变身**：string 没一键按钮，要 for 循环逐格 tolower/toupper。✅（GESP3-06 学过 tolower）
- **挑战1·找子串位置**：find + npos 判 -1。会写。
- **卡片·括号里的小动作**：`while((pos=s.find(o,pos))!=npos)` 拆解成"先存再比"。小明：这缩写我一看头大，差点跳过，看了拆解才懂。
- **checkpoint9（先存再比）**：选 A ✅。
- **挑战2·替换所有子串**：find+replace 循环，pos 跳过新内容。会写（demo 清楚）。
- **卡片·把句子拆成词**：`istringstream` 快递分拣员，`while(ss>>word)` 取词。小明：新名词又来，但比喻好懂。
- **checkpoint10（分出几个词）**：3 个 选 B ✅。
- **卡片·按逗号切开**：手动切，不是分隔符拼进小段、是分隔符输出清空、末尾多走一步。小明：和 GESP3-05 哨兵一个理，我接上了。
- **checkpoint11（多走一步为啥）**：输出最后一段 选 A ✅。
- **课后挑战5题**：闭卷。

> 小柯说方法太多他记笔记了，我哼——我 checkpoint 不也都对了，就是后边走神了两次。

## 客观测量

- **最长连续中文行**：第"按逗号切开"页「不是分隔符 → 拼进当前小段 / 是分隔符 → 小段完成，输出后清空重来 / 走到末尾再多走一步，不然最后一段会漏掉！」约 45 字；第"塞入与删除"口诀约 20 字 → **达标**（≤80）。
- **未解释新术语**：`string::npos`（"查无此串"记号）、`istringstream`（"快递分拣员"）均比喻解释；`append/insert/erase/replace` 均有 demo。整体**无完全未解释术语**。
- **示例代码行数**：教学 demo 均 ≤10 行；挑战2 解答 demo 约 14 行（答案非教学）→ **达标**。

## 独立完成测试（闭卷三步法）

| 题目 | 闭卷结果 | 对答案差异 | 判定 |
|------|----------|-----------|------|
| checkpoint1~11（string 基础操作） | 全部独立选对 | 0 | ✅×11 |
| 课后题P4 删除指定字符 | 遍历筛选拼接 ans+=s[i]（!=c） | 0 | ✅ |
| 课后题P5 查找子串并替换 | while find+replace 替换所有 | 0 | ✅ |
| 课后题P1 字符串对比 | 会用 size/==，但情况3/4需逐字符 tolower 比较拿不准 | 1 处（组合逻辑） | 🟡 |
| 课后题P2 字符串出现次数 | find 循环计数，pos 步进写错（用成+n.size） | 1 处（步进） | 🟡 |
| 课后题P3 求子串所有位置 | find 循环输出，pos 步进同样纠结+不存在输出-1 | 1 处（步进/边界） | 🟡 |

- **完成度 = (✅×1 + 🟡×0.5) / 总题数 = (13×1 + 3×0.5) / 16 = (13 + 1.5) / 16 = 14.5 / 16 ≈ 0.9**

## 卡点日志

- [append/insert/erase 段] 走神：方法密集，约 2 分钟没动手练习觉得无聊，眼睛飘走，靠"小柯都做完了"逼自己回来。
- [挑战2 缩写] 卡住：`while((pos=s.find(o,pos))!=npos)` 括号里先赋值再比，约 30 秒 → 看"括号里的小动作"卡片拆解才懂。
- [课后题P2/P3] 卡住：find 循环找所有位置/次数，pos 步进应是 +1（不是挑战2替换的 +n.size()），约 35 秒 → 看 P3 提示"pos 前进 1 格"才改对。

## 评分

- **理解率：75**（证据：11 个 checkpoint 全独立通过，string 的 +/==/size/find/replace/clear/empty/insert/erase/append/to_string/stoi/istringstream 概念均掌握；但"字符串对比情况3/4逐字符比""find 循环查所有的 pos 步进"两处组合应用半懂，属大部分懂档）
- **完成度：0.9**（按公式）
- **是否通过：是**（理解率≥75、完成度 0.9≥0.75、愿意继续——string 比 char 数组香太多了）

## 至少 3 个必须修改的问题

1. **本课方法密度过高（13+ 个接口挤在一课），小明后半程两次走神**（证据：学习日记中"append/insert/erase 段约2分钟觉得无聊走神"）。建议把"塞入/删除/append/数字变身"拆到下一课或放慢，每段后加一个"立刻试一试"小填空防走神。
2. **课后题 P1「字符串对比」情况 3/4（仅大小写不同）需要"逐字符 tolower 后比较"，但课件只给了单字符 tolower，没示范"忽略大小写比两串"**（证据：快照 P1 要区分4类，我组合时卡）。建议补一段"忽略大小写比较两个串"的示范代码。
3. **课后题 P2/P3 用 find 循环查"所有位置/次数"，pos 步进应为 +1，但课件只在 P3 提示提了一句、P2 没提，且容易和替换时的 +n.size() 搞混**（证据：我两题都把步进写错）。建议在两题题干都明确："只计数/输出位置不替换时，找到后 `pos++` 即可"，并和替换循环对比说明。

## 一句话总结（小明口吻）

"string 是弹性魔法袋，+拼接==比较不用管\\0，find/replace/erase 一堆按钮真方便！就是方法太多我后边走神了，字符串对比的大小写情况和 find 查所有的步进我还得再练。"
