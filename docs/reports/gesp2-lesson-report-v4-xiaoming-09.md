## 测试课程：GESP2-09 练习课：循环与图形打印

- **测试时长**：18 分钟
- **走神次数**：3 次（右三角形太简单走了个神；矩形边框 `bool` 函数看不懂；水仙花数之前做过有点烦）
- **想放弃次数**：1 次（边框矩形用 `isBorder` 函数，我直接抄条件反而报错，因为参数传错）
- **完成度**：90%

### 最困惑的地方
1. `bool isBorder(...)` 函数怎么和三目运算符一起用？我看了两遍。
2. 数位求和 `while` 里 `n /= 10` 为什么不能写成 `n = n / 10`？其实可以，但我不知道。

### 最喜欢的部分
1. 右三角形和等腰三角形能独立打出来，成就感爆棚。
2. 水仙花数算出四个数，我觉得自己很厉害。

### 必须修改的建议
1. `bool` 辅助函数这里可以先不加，等下节课再加，别让我一下子接受太多。
2. 每完成一题弹个小星星或音效，我现在学半天没反馈。

### 小明一句话总结
"图形和数位题复习起来顺手多了，就是函数嵌套让我想偷懒。"
