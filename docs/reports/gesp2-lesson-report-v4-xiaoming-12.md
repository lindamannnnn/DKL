## 测试课程：GESP2-12 练习课：图形打印2与存储网络

- **测试时长**：23 分钟
- **走神次数**：4 次（右对齐三角形空格和 L08 混；空心三角形分函数版本跳过不看；Z 字形之前做过；存储器分类器 `switch` 能猜到；网络分类器背口诀）
- **想放弃次数**：2 次（空心三角形函数版我看不懂，直接复制普通版；网络题把 MAN 和 WAN 搞反）
- **完成度**：85%

### 最困惑的地方
1. `spaces(k)` 和 `stars(k)` 函数版和普通版为什么要两种都展示？
2. 存储器分类器里 Cache 断电丢失还是保留？我又忘了。

### 最喜欢的部分
1. 把打印空格和星号打包成函数，代码变短了，像有了工具箱。
2. 网络分类像选城市大小，LAN 是小区、MAN 是城市、WAN 是世界。

### 必须修改的建议
1. 函数版和普通版二选一，不要同时展示，我会纠结看哪个。
2. 理论部分加"记忆宫殿"小游戏，比如拖拽存储器到正确位置。
3. 每道挑战后显示"又过一关"，让我有闯关感。

### 小明一句话总结
"这节课像复习加小测验，比纯理论好玩一点。"
