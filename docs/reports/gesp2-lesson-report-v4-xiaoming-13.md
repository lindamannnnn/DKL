## 测试课程：GESP2-13 练习课：GESP2综合大闯关

- **测试时长**：28 分钟
- **走神次数**：5 次（闰年规则太长；素数函数 `sqrt` 强转整数上限要看；数字拆解千位百位十位个位怕漏；蝴蝶图案中间空格要算；综合题步骤多）
- **想放弃次数**：3 次（闰年合并写法括号报错；`isPrime` 里 `i <= (int)sqrt(n)` 我写错成 `i < sqrt(n)`；蝴蝶图案复制后中间空格数不对）
- **完成度**：80%

### 最困惑的地方
1. 闰年"能被4整除但不能被100整除，或者能被400整除"这个条件怎么加括号？
2. 素数函数为什么循环到 `sqrt(n)`，而不是到 `n-1`？
3. 蝴蝶图案中间空格为什么是 `2*(n-i)`？

### 最喜欢的部分
1. 最大最小差一步搞定，像用了大招。
2. 数字拆解每行一个数字很解压。

### 必须修改的建议
1. 闰年先用多行 `if`，别直接给合并写法。
2. 素数函数把 `i*i <= n` 也展示，降低 `sqrt` 转整数的难度。
3. 蝴蝶图案给 `n=3` 的逐行标注，让我看到空格数怎么算。

### 小明一句话总结
"综合闯关题多但不难，就是一急就抄错括号。"
