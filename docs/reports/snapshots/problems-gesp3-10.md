# GESP3-10 课后题快照（练习课：string 综合应用）

> 来源：`docker exec dkl-postgres psql -U dkl -d dkl_db` 导出，与课件 `10-练习课：string综合应用.md` 中 `<!-- problem: uuid -->` 顺序一致。
> 导出时间：2026-07-21。题面 HTML 已清理为纯文本。
> 课件中题目顺序：86be3151 → c70aedc5 → 4fc984ac → 0ad8f4f7 → 55d1bfb8（题 5 已于 2026-07-21 替换，见题 5 节说明）。

---

## 题 1：字符串压缩
- **UUID**：`86be3151-5b16-4607-b044-b86fd006f53b`
- **来源标签**：字符串（dongfangboyi-id: 93）

**题面**：输入字符串，输出压缩后的字符串。压缩的方法是把连续的相同字母压缩为"长度+字母"的形式，在本题中，单个的字母不需要压缩。

**输入**：一行，一个字符串，只包含小写英文字母，长度不超过 255。

**输出**：（压缩后的字符串）

**输入样例**：
```
aaabbbbbx
```

**输出样例**：
```
3a5bx
```

---

## 题 2：求英文句子中的最长单词
- **UUID**：`c70aedc5-887a-4267-abd3-b2c2e4f7247f`
- **来源标签**：字符串（dongfangboyi-id: 97）

**题面**：一个英文句子（长度不超过 255），只含有字母和空格，输出最长的一个单词。如有多个长度相同的单词，则输出最前面的一个。

**输入**：一个字符串。

**输出**：一个字符串。

**输入样例**：
```
in which four coins
```

**输出样例**：
```
which
```

---

## 题 3：字符串展开
- **UUID**：`4fc984ac-bc6c-42f5-bd19-e4fb9ffd51c6`
- **来源标签**：字符串（dongfangboyi-id: 457）

**题面**：如果在输入的字符串中，含有类似于"d-h"或者"4-8"的字符子串，我们就把它当作一种简写，输出时，用连续递增的字母或数字串替代其中的减号，即将上面两个子串分别输出为"defgh"和"45678"。具体约定如下：
(1) 遇到下面的情况需要对字符串进行展开：在输入的字符串中，出现了减号"-"，减号两侧同为小写字母或同为数字，且按照 ASCII 码的顺序，减号右边的字符严格大于左边的字符。
(2) 如果减号右边的字符恰好是左边字符的后继，只删除中间的减号，例如："d-e"应输出为"de"，"3-4"应输出为"34"。如果减号右边的字符按照 ASCII 码的顺序小于或等于左边字符，输出时，要保留中间的减号，例如："d-d"应输出为"d-d"，"3-1"应输出为"3-1"。

**输入**：仅有一行包含一个长度不超过 200 的字符串，仅由数字、小写字母和减号"-"组成。行首和行末均无空格。

**输出**：仅有一行，为展开后的字符串。

**输入样例**：
```
abcs-w-y1234-9s-4zz
```

**输出样例**：
```
abcstuvwxy123456789s-4zz
```

---

## 题 4：重叠的字符串
- **UUID**：`0ad8f4f7-8212-4645-bf9b-7809895e190b`
- **来源标签**：字符串（dongfangboyi-id: 947）

**题面**：输入两个仅有小写字母组成的字符串，请求出两个字符串最长的重复部份的长度。
两个字符串的重复部份指的是一个字符串的头部的部分字符和另一个字符串的尾部的部分字符是重叠的。
比如：abcdefg 和 fggoodabcde 这两个字符串，第一个字符串的头部和第二个字符串的尾部有 5 个字符"abcde"是重叠的，第一个字符串尾部和第二个字符串的头部有 2 个字符"fg"是重叠的，因此最长的重叠部长度为 5。

**输入**：输入 2 行，每行有一个长度不超过 100 的仅由小写字母组成的字符串。

**输出**：输出两个字符串最长的重叠长度。

**输入样例**：
```
abcxxxxabcx
abcdxabcxxxxabcx
```

**输出样例**：
```
11
```

---

## 题 5：找最长单词

> 🔄 **替换说明（2026-07-21）**：原题 5《子串统计-2》（40e41590-a5b8-4395-bc41-8d4a08dd4c7e）思维要求过高，按 eval-xiaoke-gesp3-10-v1 方向"直接演练本课核心技能（find 循环计数/枚举子串入门）"替换。eval 首选《字符串出现次数》（3e108b63）已被 07 课引用，故改用本题（按空格切词 + 遍历找最长含 'a' 子串）。旧题保留在题库未删除。新题测试点已补齐为 5 个（1 可见 + 4 隐藏，每点 20 分）。

- **UUID**：`55d1bfb8-9de0-4ec8-9e0d-79ae70c4fc93`
- **来源标签**：字符串（dongfangboyi-id: 175）

**题面**：编写程序，根据给出的一个结束于 '.' 的字符字串，找出其中最长的含有字母 'a' 的子串（子串由一个或几个空格隔开）。如果有多个，只显示第一个；若没有含字母 'a' 的子串，则显示 'NO'。

**输入**：一行，为一个字符字串，结束于句点 '.'。

**输出**：一行。找出的最长的含有字母 'a' 的子串，或 'NO'。

**输入样例**：
```
Her  name is Lilan   and she  is a  happy  student.
```

**输出样例**：
```
Lilan
```
