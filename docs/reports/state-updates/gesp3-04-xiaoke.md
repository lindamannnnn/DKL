# 状态更新片段：小柯 × GESP3-04（练习课：数组基础）

> 用途：合并到知识状态文件时，把以下内容追加到"已完成的课程"和"已经理解的概念"。
> 测评日期：2026-07-21 ｜ 报告：`eval-xiaoke-gesp3-04-v1.md` ｜ 结果：**通过**（理解率 100，完成度 0.9）

## 已完成的课程（追加）

- GESP3-04 练习课：数组基础（3 checkpoint + 5 挑战 + 5 课后题，完成度 0.9）

## 评测结束后真正掌握的知识点（GESP3-04 增量）

- 01~03 复习内容全部巩固：双擂主同时找最大最小、前移删除 + `n--`、计数桶找票王（`>` 并列取小编号）、双指针真逆序
- **新增（本课挑战 5 首次讲授，非前三课内容）**："筛选装柜法"删除所有等于 x 的元素——`m = 0`，遍历原数组，`if (a[i] != x) a[++m] = a[i];`，输出前 m 个；知道旧法"发现就前移删除"对相邻重复元素会漏删
- 实战新题型经验：数组查找（遍历 + 记位置 + 找不到输出 -1 的哨兵写法）、找最大最小位置并交换、删除最小数、对称判断（`a[i]` 比 `a[n+1-i]`）、有序数组插入（先找第一个 `a[i] >= n` 的位置，再后移插入）
- 题库教训：课后题 3 输入格式缺失时，可从样例反推格式

## 还存在的薄弱点

- 课后题 5（有序插入）的边界（插到末尾 `pos=m+1`）需要样例确认才敢写，组合题熟练度不足
- "元素"一词仍是猜的（01 课遗留，未正式引入）
