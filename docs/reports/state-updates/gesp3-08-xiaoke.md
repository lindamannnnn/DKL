# 状态更新片段：小柯 × GESP3-08 练习课：字符数组与 string 入门（2026-07-21 评测后）

> 用法：合并进小柯知识状态文件时，追加到"已经理解的概念"末尾，并更新"还会困惑的点"。依据：`eval-xiaoke-gesp3-08-v1.md`。本课为练习课，无新授概念，以下为**巩固确认**清单。

## GESP3-08 练习课 之后（巩固确认，非新增）

- 含空格整行输入能自动选 `cin.getline(s, 1005)`（char 数组）/ `getline(cin, s)`（string），题面"可能含空格"会主动圈出
- 遍历统计（isupper 计数）、大小写互换（isupper→tolower / islower→toupper，其他字符不动）闭卷独立写出
- string 拼接 `a + b`、字典序比较 `a < b` 独立写出；记得不要退回 strcat/strcmp
- find 找所有位置：循环 `s.find(t, pos)` + `pos++`（允许重叠）+ found 小旗处理 -1，能独立写出（两段式写法，逻辑等价）
- replace 全部替换：知道正确前进是 `pos += n.size()` 而非 `pos++`（本次闭卷写错，对答案后掌握）
- 计数数组 cnt[26] 应用（统计字母出现次数、重复字符判断）能独立完成
- 知道字符位置题可能从 1 开始数，会对照样例检查 0/1 起点

## 新增"还会困惑的点"

- 两种 getline 写法（`cin.getline(s, 大小)` vs `getline(cin, s)`）课件未点明区别，目前靠回忆 06/07 课分清，不熟时可能混用
- `pos += n.size()` 的必要性只懂道理（新子串含旧子串时 pos++ 会错），还没亲手踩过这个坑，记忆不牢
- "第一个出现的重复字符"这类有歧义的题面，需要用样例反推，不画格子/不对样例容易理解错
- "条件反射""垃圾字符"两个词不懂词义（靠猜）

## 评测结论

- 理解率 100 / 完成度 0.9 / **通过**（小柯线 ≥75 且 ≥0.8）
