# 状态更新片段：小明 × GESP3-09 string 使用2（2026-07-21 评测后）

> 用法：合并进小明知识状态文件时，追加到"已经理解的概念"末尾，并更新"还会困惑的点"。依据：`eval-xiaoming-gesp3-09-v1.md`。

## GESP3-09 string 使用2 之后（真正掌握，细节有漏洞）

- `s.substr(起点, 个数)` 剪一段、`s.substr(起点)` 剪到尾——**但"起点从 0 数"闭卷错了两次才记住**（CP1 从 1 数选错、挑战 1 忘减 1），属于"被反馈打回两次后记住"，下次仍可能复发
- `reverse(s.begin(), s.end())` 整串倒车，能用对（CP3 ✅）
- 回文 = 正倒一样；判断套路"复制 → 倒车 → 比较"会照用（CP4 ✅），但**"为什么要复制"没细想**，属死记硬背
- 题目说"第 l 到第 r 个"要 `l - 1`、`r - l + 1`——靠样例反馈改对（🟡），不是独立写对
- 忽略大小写判回文要加 tolower 循环——漏看题干，靠样例反馈补上（🟡）

## 新增"还会困惑的点"

- substr 起点从 0 数：一着急就从 1 数（本课真实错 2 次）
- 读题漏关键字的老毛病在字符串题上集中爆发：漏"忽略大小写"（挑战 2）、漏"反向"（课后 3）、漏结尾 `.` 处理和 TRUE/FALSE 输出格式（课后 1），共 3 次
- 镜像折半建模（国王的魔镜）：放弃；分词与 `getchar()`（镜像字符串）：不认识直接关页面；重排回文规则（回文字符串-2）：想不出乱写交差
- checkpoint 三连 B 时发现"都选 B 能对"，有猜答案苗头（CP3 未细算）

## 评测结论

- 理解率 50 / 完成度 0.5 / 愿意继续 ❌ / **不通过**（小明线三项全挂：理解率 50<75、完成度 0.5<0.75、课后题三连败致徽章无望不愿继续。丢分约一半归因课后题 2/4/5 超纲配置，一半归因自身粗心与读题习惯）
