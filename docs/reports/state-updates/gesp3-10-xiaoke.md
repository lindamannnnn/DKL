# 状态更新片段：小柯 × GESP3-10 练习课：string 综合应用（2026-07-21 评测后）

> 用法：合并进小柯知识状态文件时，追加到"已经理解的概念"末尾，并更新"还会困惑的点"。依据：`eval-xiaoke-gesp3-10-v1.md`。

## GESP3-10 练习课：string 综合应用 之后（真正掌握）

- 06~09 课工具全部能闭卷调用：`getline` 读整行、`reverse(s.begin(), s.end())` 倒车、`substr(起点, 个数)` / `substr(起点)` 两种剪法、`find(t, pos)` 循环查找、`replace`、字符数组 `s[i] != '\0'` 遍历
- 固定格式字符串切分：先画"下标小尺子"再 substr（日期 `2024-03-15` → 年/月/日三段能闭卷写对）
- 允许重叠的子串计数：`find(t, pos)` 找到后 `pos++` 只前进 1 格（与"不重叠"的 `pos += t.size()` 能区分）
- 字符串旋转：`s.substr(k) + s.substr(0, k)`（后半段 + 前半段）
- 回文子串计数：外层起点 i、内层长度 len、条件 `i + len <= n` 的枚举子串套路（看 demo 学会，🟡 级掌握）
- 字符串压缩（长度+字母、单个不压缩）能独立写出，含末尾段处理
- 最长单词：getline 手工扫描切词 + 严格 `>` 保证"同长取最前"
- 头尾重叠长度：两个方向 substr 比较 + 从长到短枚举 + 找到即 break

## 新增"还会困惑的点"

- `k %= s.size()` 防越界总会忘第一次，需要错一次才想起（挑战 4 判 🟡）
- `while ((pos = s.find(t, pos)) != string::npos)` 这种"条件里赋值"的写法看得懂但不会主动写，自己还是写 break 版
- "重叠计数"概念是检查点现学现卖的，没经过正式讲解，印象浅
- "越界"一词仍无正式解释，靠 09 课"会报错"的经验猜
- 多条件叠加题（子串统计-2：枚举+计数+最长+字典序+位置五要素）超出同时处理能力，完全无法下手

## 评测结论

- 理解率 75 / 完成度 0.8（0.808）/ **通过（压线）**（小柯线 ≥75 且 ≥0.8）
- 课后题 5「子串统计-2」判定超纲，建议替换（候选：`3e108b63`、`f34dbe2c`、`616afc61`）
