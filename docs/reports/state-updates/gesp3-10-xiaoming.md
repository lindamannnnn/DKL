# 状态更新片段：小明 × GESP3-10 练习课：string 综合应用（2026-07-21 评测后）

> 用法：合并进小明知识状态文件时，追加到"已经理解的概念"末尾，并更新"还会困惑的点"。依据：`eval-xiaoming-gesp3-10-v1.md`。

## GESP3-10 练习课：string 综合应用 之后（真正掌握，比小柯少且有漏洞）

- `substr(起点, 个数)` 基本会用，但下标经常忘数分隔符（挑战 2 把月的起点写成 4）
- 字符串旋转拼接 `s.substr(k) + s.substr(0, k)` 会写，但顺序第一反应是反的；`k %= s.size()` 始终没记住
- 含空格的输入要 `getline`：被挑战 1 样例打了一次脸才记住，下次可能还犯
- "允许重叠"计数：被检查点 2 考倒、挑战 3 才学会 `pos++`，掌握不牢
- reverse、find、replace 单独用会，组合起来（挑战 5）就崩
- 照抄能理解挑战 5 的枚举子串代码，但不能独立重写（❌ 级，不算真正掌握）

## 新增"还会困惑的点"

- "重叠计数"从未被正式讲解就被考，概念是错的直觉（以为一组只能数一次）
- 双重循环边界 `i + len <= n` 和 len 起点连续写错两次，循环边界仍是重灾区
- "读到输入结束为止"（`while (cin >> w)`）完全不知道怎么办，课后题 2 卡死在输入上
- 长题面直接跳过：漏看"可能含空格""单个字母不压缩""头尾两个方向"三处关键约束
- `k %= s.size()` 防越界从没进入他的视野（"越界"一词无解释，他直接忽略）

## 评测结论

- 理解率 50 / 完成度 0.3 / 愿意继续：✗ / **不通过**（小明线 ≥75 且 ≥0.75 且愿意继续，三项全不满足）
- 本课对他难度断崖：挑战 5 崩心态，课后题 0/5。课后题 5「子串统计-2」判定超纲、题 3「字符串展开」难度超纲，建议替换（候选：`3e108b63`、`f34dbe2c`、`616afc61`、`e2261f27`、`0b24b960`）
