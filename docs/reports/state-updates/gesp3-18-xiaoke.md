# 状态更新片段：小柯 × GESP3-18 练习课：GESP3 综合大闯关（2026-07-21 评测后）

> 用法：合并进小柯知识状态文件时，追加到"已经理解的概念"末尾，并更新"还会困惑的点"。依据：`eval-xiaoke-gesp3-18-v1.md`。

## GESP3-18 综合大闯关 之后（真正掌握/巩固）

- **12 个 GESP3 知识点全部通过复习点名**：下标补 0、`strlen` 不数 `\0`、补码取反+1、逐位异或 4 个 checkpoint 闭卷全对 ✅
- **开桶计数（挑战 1 + 课后 COUNT + 数组元素的移动）稳定独立**：`cnt[x]++`、`>` 扫桶自动"并列取最小"、02 课"从前往后盖"迁移到数组移动题 ✅
- **连续段 cur/best（挑战 2）稳定独立** ✅
- **string 替换（挑战 3）🟡**：find/replace/npos 会用，但"替换后从 `p + t.size()` 继续找"会忘，需提示
- **进制+位运算综合（挑战 4）🟡**：二进制/十六进制短除法、`t & 1` + `>>=` 数 1 都稳；`n & (n-1)` 判 2 的幂**没学会**（未教），但**自创了 `cnt == 1` 替代判断**——值得鼓励的迁移能力
- **三步模拟（挑战 5）🟡**：删 0→逆序→统计主体能写，逆序输出"第一个数前不加空格"会错一次
- **字母桶 `c - 'a'`（课后题 2）🟡**：能从 `c - '0'` 迁移；"并列取 ASCII 最大"与 03 课"取最小"相反，第一反应写错、自查题意后改对

## 新增"还会困惑的点"

- **串内多位整数解析完全不会**（课后题 5 字符串解压 ❌）：`12ab10c` 里"12"怎么一次读出来没有任何课教过，`num = num*10 + (c-'0')` 的从左到右累积是空白知识点
- `n & (n-1)` 这种位运算技巧没见过，看教师卡能懂但独立想不到
- "值域"一词首次出现（03 课叫"值的范围"），靠语境猜出
- "幂"字首次出现（16 课叫"2 的 k 次方"），靠语境猜出

## 评测结论

- 理解率 75 / 完成度 0.75（10.5/14）/ **不通过（差 0.05）**（小柯线 ≥75 且 ≥0.8）
- ⚠️ 失分主因：课后题 5 字符串解压踩线超纲（❌）；挑战 4 demo 夹带未教技巧 `n&(n-1)`；挑战 3/5 各一处细节需提示
- 📌 综合课定位"不引入新概念"基本达标，唯二破口：`n&(n-1)` 与多位数解析
