# DKL 功能体检报告（2026-09-03）

> 通读 README / CLAUDE.MD / DEV_PLAN.md / CONTEXT.md / CHANGELOG.md / docs/PROJECT_OVERVIEW.md
> 后，逐项对照代码得出的实际状态。**一句话结论：文档写的完成度远高于实际实现。**

## 判定方法

不看文档的百分比，只看两处代码：

1. `client/src/App.tsx` 的路由表 — 页面存不存在
2. 每个页面里 `client.get/post(...)` 的调用 — 是真接了接口还是写死假数据

---

## 一、真正能用的

| 功能 | 状态 |
|------|------|
| 学生主链路：微课渲染 / 判题 / AI 教练 | ✅ 13 个页面都接了真实接口 |
| 后端 API | ✅ 12 个路由模块、40+ 接口齐全 |

后端比前端完整得多，**缺口主要在前端页面和接线**。

## 二、半成品

| 功能 | 问题 | 位置 |
|------|------|------|
| 题库分级浏览 | 1576 道题里 **478 道 `gespLevel` 为空**；学生端按等级入口浏览，这批题学生点不到 | `server/src/routes/problems.ts`（`where.gespLevel` + `orderBy gespLevel`） |
| 课后题测试数据 | 约 70 道被 GESP3 课件引用的题只有 1-4 个测试点 | CHANGELOG P3-3 |

## 三、假功能 / 断链（看着有，实际不能用）

| 功能 | 实际情况 | 位置 |
|------|----------|------|
| 教师·班级管理 | 页面写死 2 个假班级、假邀请码；后端 4 个接口（创建/列表/加入/学生名单）一个没调 | `client/src/pages/teacher/TeacherClassesPage.tsx`（接口调用数 = 0） |
| 教师·新建课程 | 按钮渲染了但没绑 onClick，点了没反应 | `TeacherCoursesPage.tsx` |
| 教师·课件导入 | 上传框打到 `POST /api/courses/import`，接口体就一句返回「待实现」；现成的 `markdownParser.ts`(556 行) 没接上 | `server/src/routes/courses.ts:145-147` |
| 教师·创建考试 | 路由指向内联占位 div「教师创建考试页面（待完善）」 | `App.tsx:63` |

## 四、文档点名、实际不存在的页面（6 个）

`MistakesPage`（学生错题本）、`SubmissionResultPage`（提交结果页）、`CourseImportPage`、
`ProblemEditPage`（题目录入）、`ClassDetailPage`（班级学情看板）、`TeacherReportsPage`。

其中 **学生错题本** 和 **题目录入** 的后端接口已经写好了，只差前端页面。

---

## 五、候选重做清单（按"性价比"排）

| 优先级 | 事项 | 为什么先做 |
|--------|------|-----------|
| 1 | 教师后台补全（班级接真实接口、新建课程、题目录入、班级看板） | 后端现成，主要是写前端，见效最快 |
| 2 | 课件导入打通（接 markdownParser，支持 md / zip） | 现在导入是断的，教师只能靠脚本 |
| 3 | 学生端错题本 + 提交结果页 | 后端已就绪，学生高频使用 |
| 4 | 题库可用性（478 道补定级 + 70 道补测试点） | 数据活儿，量大但机械 |
| 5 | 文档完成度同步为真实状态 | 免得下次又被文档误导 |

> 本轮未改动任何业务代码，等看完界面再定。
