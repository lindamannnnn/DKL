# AI 辅助 C++ 信奥启蒙平台 — 建设方案

## 一、项目概述

为 4-6 年级小学生及弱教资机构/个人，搭建一个 AI 辅助的 C++ 信奥学习平台。平台以 **Markdown 课件为内容驱动**，集成在线代码编辑、自动评测（JudgeServer）、AI 竞赛教练、学情反馈等完整教学闭环。

**核心公式**：
> 平台 = Markdown 课件渲染引擎 + 在线代码编辑器 + JudgeServer 判题沙箱 + AI 竞赛教练

---

## 二、目标人群

| 人群 | 特点 | 需求 |
|------|------|------|
| **4-6 年级学生** | 注意力短、抽象思维弱、打字慢、需要即时成就感 | 游戏化不是必需，但 UI 必须极简、大字号、每步有引导 |
| **弱教资机构/个人** | 自己不懂 C++、不会教 | 平台承担 90% 教学，零备课，一键开课 |
| **家长（隐形用户）** | 关心效果、不懂技术 | 每日收到孩子学了什么、哪里薄弱 |

---

## 三、课程方向：GESP C++ 1-8 级

严格对标 **CCF GESP（编程能力等级认证）C++ 1-8 级** 官方考纲。

| 级别 | 核心考点 | 课时预估 |
|------|---------|---------|
| **1级** | 变量、数据类型、运算符、表达式、输入输出、编程环境 | ~15 课时 |
| **2级** | if-else、for、while、逻辑控制、数学函数(sqrt/abs) | ~15 课时 |
| **3级** | 数组、字符串、进制转换、位运算、枚举算法 | ~15 课时 |
| **4级** | 冒泡/选择排序、函数与指针、二维数组、递推、文件读写 | ~15 课时 |
| **5级** | 贪心算法、分治算法、数论、归并排序、快速排序 | ~20 课时 |
| **6级** | DFS/BFS、一维DP/简单背包、树、哈夫曼树、栈/队列 | ~20 课时 |
| **7级** | 数学库函数、二维DP/最值优化、图遍历、图论基础、哈希表 | ~20 课时 |
| **8级** | 计数原理、排列组合、杨辉三角、倍增法、最小生成树、最短路、复杂度分析 | ~20 课时 |

**共约 160 课时，覆盖 GESP C++ 1-8 级全部考纲。**

### GESP 考试基本信息
- 满分 100 分，60 分通过
- 题型：选择题、判断题、编程题
- 编程题为 IOI 赛制（多次提交，实时评测）
- 线下机考，全部机器阅卷
- 每年 4 次（3/6/9/12 月）
- **衔接 CSP-J/S**：GESP C++ 7 级 ≥80 分 或 8 级 ≥60 分，可免 CSP-J 初赛；8 级 ≥80 分可免 CSP-S 初赛

---

## 四、技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                     前端层 (Frontend)                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         学生端 (React 18 + TypeScript + Tailwind)    │   │
│  │  • 课程大厅（大卡片式课程列表，大字号 18px+）         │   │
│  │  • 课时学习页（左侧目录 + 右侧课件渲染）              │   │
│  │    - Markdown 渲染（图文、表格、代码块）              │   │
│  │    - 可运行代码块（一键运行，看输出）                 │   │
│  │    - 内嵌选择题/填空题（即时反馈）                    │   │
│  │  • 编程练习页（题面 + Monaco 编辑器 + 提交评测）      │   │
│  │  • AI 教练侧边栏（常驻，结合当前课件上下文）          │   │
│  │  • 个人仪表盘（进度、正确率、今日任务）               │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         教师/机构后台 (React + Ant Design)           │   │
│  │  • 课件上传（Markdown / ZIP 批量导入）                │   │
│  │  • 课程结构管理（章节/课时排序、预览）                │   │
│  │  • 题库管理（题目录入、测试用例配置）                 │   │
│  │  • 班级管理 / 学生进度监控 / 每日报告                 │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     后端层 (Backend)                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │      核心服务 (Node.js + Express + Prisma + PG)      │   │
│  │  用户 / 班级 / 课程 / 课时 / 题目 / 测试用例          │   │
│  │  提交记录 / 学习进度 / AI 对话 / 报告生成             │   │
│  │  课件解析服务（MD → 结构化 JSON）                     │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     基础设施层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ JudgeServer  │  │   PostgreSQL │  │   Redis          │  │
│  │ (Docker沙箱) │  │   (主数据库) │  │   会话/AI记忆    │  │
│  │              │  │              │  │   /评测队列      │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              大模型 API (OpenAI/Claude/DeepSeek)      │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 技术选型说明

| 层级 | 选型 | 理由 |
|------|------|------|
| 前端 | React 18 + TypeScript + Tailwind CSS + Vite | 现代、类型安全、样式高效 |
| 编辑器 | Monaco Editor | VS Code 同款，支持 C++ 语法高亮和智能提示 |
| 后端 | Node.js + Express + Prisma | JavaScript 全栈，Prisma 自动生成 CRUD |
| 数据库 | PostgreSQL | 关系型，支持 JSON 字段存课件内容 |
| 缓存 | Redis | 会话、AI 对话上下文、评测队列 |
| 判题 | JudgeServer (Docker) | QDUOJ 开源判题核心，成熟稳定 |
| AI | 大模型 API | GPT-4 / Claude / DeepSeek，按 token 计费 |

---

## 五、Markdown 课件规范

用户按以下规范编写 Markdown，平台自动解析为互动课件。

### 示例课件

```markdown
# 第1章 GESP 1级：你好C++

## 课时1-1 第一个程序

今天我们要学习让电脑说话！

在 C++ 中，我们使用 `cout` 来输出内容。

```cpp
#include <iostream>
using namespace std;
int main() {
    cout << "Hello, C++!";
    return 0;
}
```
<!-- run -->
<!-- 标记此代码块可一键运行，学生点击后弹出编辑器预填代码 -->

> 💡 教练提示：每一句话末尾都要加英文分号 `;`，就像句号一样！

---

### 练一练

<!-- quiz: choice -->
上面程序的输出是什么？
- A. Hello, C++!
- B. hello, c++!
- C. 什么都不输出
<!-- answer: A -->

---

### 课后编程题

<!-- problem: gesp1-001 -->
**题目**：请编写程序输出 `I love programming.`

**输入**：无
**输出**：`I love programming.`
```

### 支持的 HTML 注释标记

| 标记 | 功能 |
|------|------|
| `<!-- run -->` | 代码块可一键运行/复制到练习区 |
| `<!-- quiz: choice -->` | 单选题（A/B/C/D） |
| `<!-- quiz: fill -->` | 填空题 |
| `<!-- problem: {id} -->` | 关联编程题（引用题库中的题目） |
| `<!-- hint -->` | 提示折叠区 |
| `<!-- coach-tip -->` | AI 教练会在此时主动提示的内容 |

### 解析规则

- `# 标题` → 章节（Chapter）
- `## 标题` → 课时（Lesson）
- 普通内容 → 图文教程（Markdown 渲染）
- `<!-- run -->` 的代码块 → 可运行示例（带「运行」按钮）
- `<!-- quiz -->` → 互动题（前端即时判对错）
- `<!-- problem: id -->` → 关联编程题（id 需在题库中存在）

---

## 六、核心模块设计

### 6.1 课件导入系统

1. 教师登录后台 → 上传 Markdown（单文件或 Zip 批量）
2. 后端解析 Markdown → 提取章节/课时/内容块
3. 存入数据库（lessons 表的 content 字段存 JSON 数组）
4. 教师检查解析结果，调整顺序
5. 在题库中录入编程题及测试用例
6. 在课件中补充 `<!-- problem: id -->` 标记关联题目
7. 创建班级 → 下发课程 → 分享邀请码给学生

### 6.2 学生端体验

| 页面 | 设计要点 |
|------|---------|
| **课程大厅** | 大卡片展示各课程，进度条直观，字体 18px+，色彩柔和不刺眼 |
| **课时页** | 左目录树 + 右内容区。Markdown 清晰渲染，代码块带「运行」按钮 |
| **互动题** | 选择题点击即反馈对错，答错显示解析；填空题自动判对错 |
| **编程题页** | 题面在上（含输入输出样例），编辑器在下。预填模板代码，实时检测中文标点 |
| **提交结果** | ❌ 不用 AC/WA/RE/TLE 等专业缩写<br>✅ 用「全部通过 / 部分通过(x/y) / 编译出错」+ AI 解释 |
| **AI 教练** | 侧边栏常驻，结合当前课时内容回答。学生卡壳时主动介入 |
| **个人仪表盘** | 学习日历、正确率趋势图、已掌握知识点、今日任务 |

### 6.3 代码编辑器特殊配置

- **主题**：柔和浅色（护眼），可选深色模式
- **字号**：默认 16px+
- **自动补全**：简化，只补全基础关键字和已定义变量
- **实时纠错**：输入中文分号/括号时自动标红并提示「请用英文标点哦」
- **代码模板**：每道题预填 `#include <bits/stdc++.h>` 和 `int main(){}` 框架

### 6.4 AI 竞赛教练

**人设：信奥教练**（不是小精灵，不是机器人，是一位耐心的竞赛老师）

**核心场景**：

| 场景 | AI 行为 |
|------|---------|
| **课时学习** | 学生阅读课件时随时提问，AI 结合当前课时内容回答 |
| **编译错误** | 自动解析 g++ 错误，用小学生能懂的话解释：「你在第3行忘了加分号，就像一句话忘了句号，电脑读不懂啦」 |
| **答案错误** | 不直接给答案，提示测试边界：「试试输入 n=1，看看输出什么？」 |
| **超时(TLE)** | 「你的方法是对的，但是跑得太慢了。想想有没有办法少做重复计算？」 |
| **卡壳检测** | 同一题提交 3 次未通过 → AI 主动弹出：「需要教练给你一点提示吗？」 |
| **每日总结** | 「今天你太棒了！完成了5道题，for 循环掌握得很扎实，明天继续加油！」 |

**AI 提示词核心原则**：
```
你是信奥教练，教 4-6 年级小学生 C++。
规则：
1. 绝不说教，用比喻和短句
2. 学生要答案时，用提问引导他自己想
3. 每次回复不超过 3 句话
4. 中英文标点混淆时直接指出并纠正
5. 发现学生沮丧立刻鼓励
```

### 6.5 题目与评测系统

**重要：JudgeServer 本身不带任何题目和测试数据！**

- JudgeServer 只是一个「判题沙箱」：你发代码+测试数据给它，它编译运行并返回结果
- 平台的「题库管理系统」必须自建
- 教师在后台录入：题面、模板代码、测试用例（输入/期望输出/是否隐藏）
- 支持批量导入题目（JSON / Excel 格式）
- 建议配套录入 GESP 历年真题（CCF 官方有公开真题）

**评测反馈优化**：
```
❌ 不显示：
  Time Limit Exceeded on test 5
  Wrong Answer on test 3

✅ 改为显示：
  「前 2 个测试点通过了，第 3 个测试点没通过。
   你的输出是 15，但正确答案是 21。
   想想看，是不是漏加了什么？」
```

### 6.6 教师/机构后台

| 功能 | 说明 |
|------|------|
| **课件上传** | 单文件 / Zip 批量导入 Markdown 课件 |
| **课程管理** | 调整章节/课时顺序，预览课件渲染效果 |
| **题库管理** | 录入编程题、配置测试用例（输入/期望输出/是否隐藏） |
| **班级管理** | 创建班级、邀请学生、下发课程 |
| **进度监控** | 看板显示每个学生学到哪一课、完成度 |
| **每日报告** | AI 自动生成班级学情摘要（白话版） |
| **零备课辅助** | 每节课自动生成「给老师的 1 分钟摘要」（重点、易错点、鼓励话术） |

---

## 七、数据模型

```
users                    -- 用户（学生/教师/管理员）
  - id, username, email, role, password_hash, created_at

classes                  -- 班级
  - id, name, teacher_id, invite_code, created_at

class_students           -- 班级学生关系
  - class_id, student_id, joined_at

courses                  -- 课程
  - id, title, description, level_min, level_max, status

chapters                 -- 章节
  - id, course_id, title, sort_order

lessons                  -- 课时
  - id, chapter_id, title, sort_order
  - content: JSON        -- 解析后的课件内容块数组
  - raw_markdown: TEXT   -- 原始 Markdown（便于重新解析）

problems                 -- 编程题
  - id, title, description, input_desc, output_desc
  - sample_input, sample_output, sample_explanation
  - starter_code, difficulty, tags, time_limit, memory_limit

test_cases               -- 测试用例
  - id, problem_id, input, expected_output, is_hidden

submissions              -- 提交记录
  - id, user_id, problem_id, code, language
  - result, passed_count, total_count, time_used, memory_used
  - ai_feedback, created_at

learning_progress        -- 课时学习进度
  - user_id, lesson_id, status, completed_at

ai_conversations         -- AI 对话历史
  - id, user_id, lesson_id, role, content, created_at

feedback_reports         -- AI 生成的学习报告
  - id, user_id, report_date, content, stats_json
```

---

## 八、核心流程

### 教师配置流程

```
登录后台
  → 上传 Markdown 课件（单文件或 Zip）
  → 平台自动解析为课程/章节/课时
  → 教师检查解析结果，调整顺序
  → 在题库中录入编程题及测试用例
  → 在课件中补充 <!-- problem: id --> 标记关联题目
  → 创建班级
  → 下发课程
  → 分享邀请码给学生
```

### 学生学习流程

```
登录
  → 看到「今日任务」（推荐题 + 新课提醒）
  → 进入课程 → 浏览章节 → 进入课时
  → 阅读 Markdown 课件
  → 看到代码示例 → 点击「运行」→ 弹出编辑器执行看结果
  → 做内嵌选择题/填空题 → 即时反馈
  → 课时结束 → 进入课后编程题
  → 编写代码 → 点击「运行」（跑样例）
  → 确认无误 → 点击「提交」（跑全部测试点）
  → AI 自动反馈结果
  → 全对 → 鼓励语 + 解锁下一课时
  → 有错 → AI 分析并给出引导提示
  → 完成今日任务 → 仪表盘更新
  → 每日 21:00 自动生成学习报告
```

### AI 报告生成流程

```
系统每日 21:00 汇总学生数据
  → 课时完成记录
  → 代码提交记录（正确率、错误类型、卡关位置）
  → 构造 prompt 发给大模型
  → 生成白话版学习摘要
  → 存入数据库
  → 家长端/邮件推送
```

---

## 九、目录结构（Monorepo）

```
DKL/
├── docker-compose.yml          # 基础设施一键启动
├── plan.md                     # 本方案文档
├── README.md                   # 项目说明
│
├── server/                     # 后端服务
│   ├── src/
│   │   ├── index.ts            # 入口
│   │   ├── routes/             # API 路由
│   │   ├── services/           # 业务逻辑
│   │   ├── parsers/            # Markdown 课件解析器
│   │   └── utils/              # 工具函数
│   ├── prisma/
│   │   └── schema.prisma       # 数据库模型
│   ├── package.json
│   └── tsconfig.json
│
├── client/                     # 学生端前端
│   ├── src/
│   │   ├── pages/              # 页面
│   │   ├── components/         # 组件
│   │   ├── stores/             # 状态管理
│   │   └── api/                # API 客户端
│   ├── package.json
│   └── vite.config.ts
│
├── admin/                      # 教师后台前端
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
│
└── sandbox/                    # JudgeServer 配置
    └── docker-compose.yml
```

---

## 十、实施阶段

| 阶段 | 核心任务 | 产出 |
|------|---------|------|
| **Phase 1** | 创建项目结构，Docker 部署 JudgeServer + PostgreSQL + Redis | 基础设施可运行 |
| **Phase 2** | 后端核心 API：用户/课程/课时/题目/提交/评测/AI 对话 | 后端可独立测试 |
| **Phase 3** | 学生端前端：课程大厅/课时渲染/编辑器/提交/AI 教练 | 学生可完整上课做题 |
| **Phase 4** | 教师后台：课件上传解析/题库管理/班级/进度/报告 | 机构可开课管理 |
| **Phase 5** | 内置题库：录入 GESP 1-4 级配套题目（约 60 道）+ 真题 | 有内容可学 |
| **Phase 6** | 联调测试、UI 打磨、性能优化、部署文档 | 上线版本 |

---

## 十一、开源组件说明

| 组件 | 来源 | 说明 |
|------|------|------|
| **JudgeServer** | [QingdaoU/JudgeServer](https://github.com/QingdaoU/JudgeServer) | QDUOJ 的独立判题微服务，Go 语言编写，Docker 沙箱编译运行 C++ |
| **Monaco Editor** | Microsoft | VS Code 同款编辑器，支持 C++ 语法高亮和智能提示 |
| **shadcn/ui** | 开源组件库 | 基于 Tailwind 的 React 组件，加速 UI 开发 |

---

## 十二、注意事项

1. **JudgeServer 不带题库**：题目和测试用例必须自建，这是平台的重点工作之一
2. **GESP 真题可用**：CCF 官网公开了历年真题，可作为平台首批题目来源
3. **小学生特殊设计**：大字号、简洁 UI、即时反馈、AI 主动介入，不是传统 OJ 的冷冰冰体验
4. **弱教资机构是核心用户**：后台必须做到「上传课件就能开课」，零技术门槛
