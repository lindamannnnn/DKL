# CSP02-10 C2阶段测试

## 考试说明

**总分**：100分  
**时间**：90分钟  
**题型**：选择题（20分）+ 判断题（20分）+ 编程题（60分）  
**注意事项**：
- 编程题请将代码写在答题纸的对应位置（或提交至在线评测系统）
- 代码需能编译通过，注意格式
- 样例数据必须验证正确

---

## 第一部分：选择题（每题2分，共10题，共20分）

**1.** 以下函数中，返回值类型为 `double` 的是？

A. `abs(-5)`　　B. `max(3, 7)`　　C. `sqrt(9.0)`　　D. `rand()`

---

**2.** 执行以下代码后，变量 `a` 的值是？

```cpp
void f(int x) { x *= 2; }
int main() {
    int a = 5;
    f(a);
    cout << a;
}
```

A. 10　　B. 5　　C. 0　　D. 编译错误

---

**3.** 字符 `'D'` 的 ASCII 值是？（`'A'` = 65）

A. 67　　B. 68　　C. 69　　D. 70

---

**4.** 以下代码输出什么？

```cpp
int a = 9, b = 4;
cout << (double)a / b;
```

A. `2`　　B. `2.0`　　C. `2.25`　　D. `2.5`

---

**5.** 在 n=5 的等腰三角形中，第3行有多少个 `*`？

A. 3　　B. 5　　C. 7　　D. 6

---

**6.** TCP/IP 四层模型中，HTTP 协议属于哪一层？

A. 网络层　　B. 传输层　　C. 应用层　　D. 网络接口层

---

**7.** 以下关于 OSI 七层模型的描述中，正确的是？

A. TCP 协议属于网络层  
B. IP 协议属于传输层  
C. 物理层是第七层  
D. 数据链路层负责帧传输和 MAC 地址寻址  

---

**8.** 以下代码，当 a=3, b=-2 时，输出什么？

```cpp
if (a > 0)
    if (b > 0) cout << "A";
    else cout << "B";
else cout << "C";
```

A. A　　B. B　　C. C　　D. 无输出

---

**9.** `rand() % 100` 生成的随机整数范围是？

A. `[1, 100]`　　B. `[0, 99]`　　C. `[0, 100]`　　D. `[1, 99]`

---

**10.** 关于计算机存储，以下描述**错误**的是？

A. Cache 位于 CPU 内部或紧邻 CPU，速度最快  
B. RAM 断电后数据丢失  
C. ROM 可以随意读写更改  
D. 硬盘的容量比内存大得多  

---

## 第二部分：判断题（每题2分，共10题，共20分）

1. `floor(-3.2)` 的结果是 -3。（　）

2. 函数的形参是实参的拷贝，修改形参不影响实参。（　）

3. `'a' - 'A' == 32`。（　）

4. 局域网（LAN）覆盖范围可以跨越多个国家。（　）

5. IP 地址 `192.168.0.1` 属于 C 类地址。（　）

6. `switch` 语句中，如果没有 `break`，匹配的 `case` 执行后会继续向下执行。（　）

7. `srand(time(0))` 的作用是以当前时间为随机数种子，保证每次运行结果不同。（　）

8. 全局变量的默认初始值是 0，而局部变量的默认初始值是随机值。（　）

9. Python 是解释型语言，C++ 是编译型语言。（　）

10. 等腰三角形第 i 行（共n行）的总字符宽度（空格+星号）为 `2*n - 1`，是固定值。（　）

---

## 第三部分：编程题（共3题，共60分）

---

### 编程题 1（20分）：数字拆解

**题目描述**：

输入一个正整数 n（100 ≤ n ≤ 9999），分别输出它的每一位数字（个位、十位、百位、千位，没有的位输出 0），每个数字占一行。

**输入格式**：一个正整数 n。

**输出格式**：4 行，依次输出千位、百位、十位、个位。

**样例输入**：
```
1234
```

**样例输出**：
```
1
2
3
4
```

**样例输入2**：
```
57
```

**样例输出2**：
```
0
0
5
7
```

---

### 编程题 2（20分）：自定义函数求素数

**题目描述**：

定义一个函数 `bool isPrime(int n)` 判断 n 是否为素数。输入整数 L 和 R（2 ≤ L ≤ R ≤ 1000），输出 L 到 R 范围内所有素数，每个素数占一行。

**输入格式**：两个整数 L 和 R，用空格分隔。

**输出格式**：每行输出一个素数。

**样例输入**：
```
10 20
```

**样例输出**：
```
11
13
17
19
```

---

### 编程题 3（20分）：图形打印

**题目描述**：

输入正整数 n，打印如下图案（n=4 为例）：

```
*   *
**  **  
*** ***
*******
```

说明：第 i 行（i 从 1 到 n）：
- 左边 i 个 `*`
- 中间 `2*(n-i)` 个空格（第 n 行中间无空格，连在一起）  
- 右边 i 个 `*`

**输入格式**：一个正整数 n（2 ≤ n ≤ 10）。

**输出格式**：n 行，打印如上图案。

**样例输入**：
```
3
```

**样例输出**：
```
*  *
** **
*****
```

---

## 参考答案

### 选择题答案

| 题号 | 答案 | 解析 |
|------|------|------|
| 1 | C | `sqrt()` 返回 double，其余返回 int |
| 2 | B | 值传递，实参不变 |
| 3 | B | 'A'=65，'D'=65+3=68 |
| 4 | C | `(double)9/4 = 2.25` |
| 5 | B | 第3行：`2*3-1=5` 个 `*` |
| 6 | C | HTTP 是应用层协议 |
| 7 | D | 数据链路层负责帧传输和MAC寻址 |
| 8 | B | else 属于内层 if，a>0且b<=0时输出B |
| 9 | B | `rand()%100` 范围是 [0,99] |
| 10 | C | ROM 是**只读**存储器，不能随意写 |

### 判断题答案

| 题号 | 答案 | 解析 |
|------|------|------|
| 1 | ✗ | `floor(-3.2)=-4.0`（向下取整） |
| 2 | ✓ | 值传递的本质 |
| 3 | ✓ | 大小写 ASCII 差值为 32 |
| 4 | ✗ | LAN 是局域网，范围是校园/公司级别 |
| 5 | ✓ | 192 开头，C 类地址 |
| 6 | ✓ | switch 穿透特性 |
| 7 | ✓ | srand 设置种子 |
| 8 | ✓ | 全局变量默认0，局部变量随机 |
| 9 | ✓ | 正确分类 |
| 10 | ✓ | 总宽度 = `2*n-1`，固定 |

### 编程题参考代码

**编程题 1 参考代码**：

```cpp
#include <iostream>
using namespace std;
int main() {
    int n; cin >> n;
    cout << n / 1000 << endl;
    cout << n / 100 % 10 << endl;
    cout << n / 10 % 10 << endl;
    cout << n % 10 << endl;
    return 0;
}
```

**编程题 2 参考代码**：

```cpp
#include <iostream>
#include <cmath>
using namespace std;
bool isPrime(int n) {
    if (n < 2) return false;
    for (int i = 2; i <= (int)sqrt(n); i++)
        if (n % i == 0) return false;
    return true;
}
int main() {
    int L, R; cin >> L >> R;
    for (int i = L; i <= R; i++)
        if (isPrime(i)) cout << i << endl;
    return 0;
}
```

**编程题 3 参考代码**：

```cpp
#include <iostream>
using namespace std;
int main() {
    int n; cin >> n;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= i; j++) cout << "*";
        for (int j = 1; j <= 2*(n-i); j++) cout << " ";
        for (int j = 1; j <= i; j++) cout << "*";
        cout << endl;
    }
    return 0;
}
```

---

## 测试后复盘（考试结束后，5分钟）

**👩‍🏫 教师引导：**

> "恭喜大家完成 C2 阶段测试！
>
> 这是你们本阶段学习成果的最终答卷——成绩很重要，但更重要的是**看清楚哪里还有不足**。
>
> 批改完试卷后，整理一份'错题清单'，针对性地回顾那些知识点！
>
> C2 阶段到此结束——下一个阶段，更精彩的内容在等着你们！"
